# Handover: MVP Aplikasi Web Orientasi Panitia

Dokumen ini adalah rencana lengkap yang sudah **disetujui** untuk pembangunan MVP. Tiap fase akan dikerjakan di session Claude Code yang berbeda-beda — sebelum mulai kerja di fase manapun, baca dokumen ini dulu untuk konteks penuh, lalu cek bagian **Status Progres** di bagian bawah untuk tahu fase mana yang harus dikerjakan berikutnya.

## 1. Konteks Produk

Aplikasi web internal untuk mengelola kepanitiaan event (misal: **FIND**, **PON**) yang berjalan di beberapa **site** sekaligus dan paralel (misal FIND berjalan di site Ciawi-Sentul, Cibitung, dan Jakarta-Area; PON berjalan di site Ciawi, Sentul, Cibitung). Setiap kombinasi kepanitiaan+site adalah unit kerja independen dengan susunan panitia, timeline, tugas, dan budgeting sendiri-sendiri.

Ada dua jenis akun:
- **Akun panitia** — satu akun per instance kepanitiaan+site, akses terbatas ke datanya sendiri
- **Akun leader** (dipegang Yolanda) — akses ke semua instance, lihat progres lintas kepanitiaan/site lewat Dashboard Kepanitiaan

## 2. Fitur Wajib (Scope MVP — JANGAN ditambah di luar ini)

1. Page susunan panitia per instance kepanitiaan+site
2. Timeline pelaksanaan per instance kepanitiaan+site
3. Bucket kepanitiaan per bidang (6 bucket default + bisa tambah custom), berisi tugas besar + breakdown subtugas + deadline masing-masing
4. Download template budgeting & submit budget per kepanitiaan
5. Akun per kepanitiaan (panitia) + Dashboard Kepanitiaan untuk leader (multi-select instance yang mau direview)
6. Desain clean tapi fun khususnya di elemen reminder pengisian
7. Notifikasi berkala (reminder ke panitia menjelang deadline)
8. Notifikasi otomatis ke leader saat: (a) budget submission lengkap, (b) seluruh tugas dalam satu instance kepanitiaan selesai 100%

## 3. Fitur yang SENGAJA Ditunda (jangan dibangun dulu)

- Submit notulensi meeting
- Home berupa game path perjalanan (rumah → jungle → gunung) dengan progress journey panitia
- Leaderboard pengisian antar-site untuk kepanitiaan yang jalan paralel

## 4. Tech Stack

| Layer | Pilihan |
|---|---|
| Framework | Next.js (App Router) + TypeScript |
| Database + Auth + Storage | Supabase (Postgres, Auth, Storage, RLS) |
| Styling | Tailwind CSS + shadcn/ui |
| Notifikasi terjadwal | Supabase Edge Functions + pg_cron (atau Vercel Cron) |
| Notifikasi delivery | Email via Resend (MVP; WhatsApp/Telegram bisa upgrade nanti) |
| Deploy | Vercel |
| Asset desain fun (opsional) | Canva (via MCP) untuk ilustrasi/banner reminder |

## 5. Model Data

```sql
sites               (id, nama_site)
kepanitiaan         (id, nama)                                -- FIND, PON, dst
kepanitiaan_site    (id, kepanitiaan_id, site_id)              -- instance: "FIND @ Cibitung"
committee_members   (id, kepanitiaan_site_id, nama, email, role)  -- role: 'anggota' | 'leader_bidang'
buckets             (id, kepanitiaan_site_id, nama_bidang, is_default, is_budgeting)
tasks               (id, bucket_id, judul, deadline, status)
subtasks            (id, task_id, judul, deadline, status)
timeline_milestones (id, kepanitiaan_site_id, nama_milestone, tanggal_mulai, tanggal_selesai)
budget_submissions  (id, kepanitiaan_site_id, file_url, status, submitted_at)
users               (id, email, role: 'panitia' | 'leader', kepanitiaan_site_id nullable)
notifications_log   (id, kepanitiaan_site_id, jenis, sent_at)
```

Catatan penting:
- `kepanitiaan` dan `sites` adalah entitas independen. Semua data operasional (susunan panitia, timeline, bucket, budget) menempel ke `kepanitiaan_site` (instance gabungan), **bukan** ke `sites` saja.
- 5 bucket default di-seed otomatis setiap kali instance `kepanitiaan_site` baru dibuat: **Budgeting, Acara, Perlengkapan/Logistik/Transportasi, Publikasi dan Dokumentasi, Konsumsi**. Bucket "Budgeting" ditandai `is_budgeting = true` agar UI-nya menampilkan widget download-template + upload-submission (bukan cuma daftar tugas biasa). Panitia bisa tambah bucket custom lain (`is_default = false`). (Sebelumnya ada 6 bucket termasuk "Penetapan Susunan Kepanitiaan" — dihapus di revisi Fase 3 karena itu seharusnya jadi tugas di dalam bucket lain, bukan bidang/bucket tersendiri. Lihat migration `20260809000005_fase3_hapus_bucket_penetapan_susunan.sql`.)
- RLS Supabase: akun panitia hanya bisa akses baris yang terhubung ke `kepanitiaan_site_id` miliknya; akun leader bisa akses semua.

## 6. Urutan Pembangunan (kerjakan berurutan, satu fase = satu session)

### Fase 0 — Setup Project
- Init Next.js + TypeScript + Tailwind + shadcn/ui
- Setup project Supabase, simpan env keys (`.env.local`, jangan commit)
- Struktur folder: `app/(auth)`, `app/(panitia)`, `app/(leader)`, `lib/supabase`
- **Selesai jika:** project bisa `next dev` dan connect ke Supabase tanpa error

### Fase 1 — Auth & Skema Data
- Migration SQL untuk semua tabel di atas (section 5) + RLS policies sesuai catatan RLS
- Halaman login (akun panitia + akun leader)
- Middleware redirect berdasarkan role
- **Selesai jika:** login sukses, redirect sesuai role, RLS teruji (akun panitia A tidak bisa lihat data instance B)

### Fase 2 — Manajemen Kepanitiaan & Susunan Panitia
- Form leader: buat kepanitiaan baru → pilih site-site yang menjalankan → sistem otomatis generate instance `kepanitiaan_site` per site + auto-seed 6 bucket default
- Halaman susunan panitia (CRUD anggota) per instance
- **Selesai jika:** leader bisa membuat kepanitiaan "FIND" dengan 3 site sekaligus dan otomatis muncul 3 instance dengan bucket default masing-masing

### Fase 3 — Timeline Pelaksanaan
- Halaman timeline per instance `kepanitiaan_site` (vertical timeline sederhana)
- **Selesai jika:** timeline FIND@Cibitung dan FIND@Jakarta-Area independen satu sama lain

### Fase 4 — Bucket Tugas & Breakdown
- Halaman per bucket: daftar tugas besar → expand ke subtugas, masing-masing dengan deadline + status
- Tombol "+ Tambah Bidang" untuk bucket custom
- Progress bar sederhana per bucket
- **Selesai jika:** panitia bisa CRUD tugas/subtugas dan menambah bucket baru di instance-nya sendiri

### Fase 5 — Download/Submit Template Budgeting
- Bucket "Budgeting" (`is_budgeting=true`) render widget khusus: tombol download template (file statis di Supabase Storage) + form upload submission
- Status submission: belum / submitted lengkap
- **Selesai jika:** panitia bisa download template, upload file, dan statusnya berubah jadi lengkap

### Fase 6 — Dashboard Kepanitiaan (Leader)
- Landing page leader: checklist multi-select semua instance `kepanitiaan_site` (dikelompokkan per nama kepanitiaan)
- Dashboard menampilkan ringkasan progress gabungan (status tugas per bucket, status budgeting) untuk instance yang dicentang
- **Selesai jika:** leader bisa pilih beberapa instance sekaligus dan lihat perbandingan progres side-by-side

### Fase 7 — Notifikasi
- Reminder berkala ke panitia (cron cek deadline task/subtask mendekat, belum selesai) via email
- Notifikasi event-based ke leader: trigger saat `budget_submissions.status` → lengkap, dan saat seluruh task+subtask dalam satu instance berstatus selesai (100%)
- **Selesai jika:** email reminder terkirim sesuai jadwal, dan notif leader muncul tepat saat trigger terjadi (bukan polling manual)

### Fase 8 — Polish Desain
- Terapkan Tailwind + shadcn/ui secara konsisten, fokus di elemen reminder biar terasa "fun" (warna ceria, copy yang playful) tanpa menambah fitur gamifikasi
- Opsional: generate 2-3 asset ilustrasi/banner via Canva untuk elemen reminder
- **Selesai jika:** review visual QA — semua halaman utama (susunan panitia, timeline, bucket tugas, budgeting, dashboard) terasa konsisten dan clean

## 7. Fitur yang Sengaja Tidak Disiapkan Struktur Datanya

Jangan tambahkan tabel/kolom untuk: notulensi meeting, game path journey, leaderboard antar-site. Ini di luar scope MVP dan akan dibahas terpisah nanti.

## 8. Status Progres

> Update bagian ini setiap kali sebuah fase selesai dikerjakan, supaya session berikutnya tahu harus mulai dari mana.

- [x] Fase 0 — Setup Project (kode & struktur selesai; project Supabase sudah dibuat, `.env.local` terisi kredensial asli, `next build`/`next dev` sukses tanpa error dan sudah diverifikasi jalan lokal)
- [x] Fase 1 — Auth & Skema Data (migration SQL sudah dijalankan ke Supabase asli, RLS sudah diuji manual dan terbukti panitia A tidak bisa lihat data instance B, login lewat `npm run dev` sudah dicoba dan redirect sesuai role sukses untuk akun panitia maupun leader — diverifikasi langsung oleh Yolanda)
- [x] Fase 2 — Manajemen Kepanitiaan & Susunan Panitia (kedua migration sudah dijalankan ke Supabase asli dan diverifikasi live oleh Yolanda: form leader bisa membuat kepanitiaan multi-site, tiap instance otomatis dapat bucket default, menambah site ke kepanitiaan yang sudah ada juga jalan, dan CRUD susunan panitia berfungsi. Jumlah bucket default sekarang 5, bukan 6 lagi — lihat revisi ketiga Fase 3. Isolasi RLS `committee_members` dari sisi akun panitia **sudah dikonfirmasi live di sesi Fase 3** — lihat `supabase/verify_fase3.sql` — jadi celah `verify_fase2.sql` poin 7-8 sudah tertutup)
- [x] Fase 3 — Timeline Pelaksanaan — **selesai & terverifikasi live sepenuhnya**, tidak ada yang menggantung. Kode termasuk 3 ronde revisi + 2 bugfix: flag status milestone, Gantt chart mingguan (+ bugfix drift tanggal & bugfix lebar bar/warna status), edit-lock milestone, bidang untuk leader_bidang, dan hapus bucket default "Penetapan Susunan Kepanitiaan". Kedua migration baru (`20260809000004`, `20260809000005`) sudah dijalankan. Independensi timeline antar-instance dan isolasi RLS `timeline_milestones_scoped`/`committee_members_scoped` dari sisi akun panitia (pakai `panitia-a@test.local`/`panitia-b@test.local`, disambungkan ke instance asli lewat `verify_fase3.sql`) **sudah dikonfirmasi**: akun panitia login benar, cuma lihat instance sendiri, bisa CRUD susunan panitia & timeline miliknya sendiri.
- [x] Fase 4 — Bucket Tugas & Breakdown — **selesai & terverifikasi live sepenuhnya**, tidak ada yang menggantung. Kode lewat **5 ronde revisi live** dengan Yolanda — lihat rincian lengkap di Catatan Teknis Fase 4 di bawah. Board Trello, assign anggota, upload logo kepanitiaan, tab menyamping untuk leader & panitia, tab "Tugas Saya", pembatasan tambah-anggota ke akun terdaftar, dan rename aplikasi jadi **PanitiYAY** semua sudah dikonfirmasi live. **Isolasi RLS `tasks_scoped`/`subtasks_scoped` dari sisi akun panitia juga sudah dikonfirmasi**: `panitia-e@test.local`/`panitia-d@test.local` (di 2 instance berbeda — `panitia-e` dibuat menggantikan `panitia-c` karena akun `panitia-c` lama sempat terkendala login, lihat catatan di bawah) dites lengkap 3 langkah — (a) panitia-e tambah tugas percobaan di instance-nya, (b) panitia-d login dan **tidak** melihat tugas itu sama sekali (baik di Task Board maupun Tugas Saya), (c) panitia-d coba akses langsung URL bucket milik instance panitia-e lewat address bar dan **kena 404**, bukan bocor data. Ketiganya lolos.
- [x] Fase 5 — Download/Submit Template Budgeting
- [ ] Fase 6 — Dashboard Kepanitiaan (Leader)
- [ ] Fase 7 — Notifikasi
- [ ] Fase 8 — Polish Desain

**Fase 5 -- kode sudah lewat beberapa ronde revisi live dengan Yolanda dan migration-nya sudah jalan, TAPI verifikasi live end-to-end (download template + upload submission + status berubah jadi "Lengkap") dengan tampilan FINAL belum dikonfirmasi eksplisit -- jangan dicentang selesai sebelum checklist di Catatan Teknis Fase 5 tuntas.** Ringkasan perjalanan revisinya (detail lengkap tiap poin ada di Catatan Teknis Fase 5):
1. Build awal: widget berdampingan dengan Task Board di bucket "Budgeting" (bukan menggantikan), pakai file placeholder (`supabase/seed/template-budgeting-placeholder.xlsx`) karena template asli belum ada -- 2 keputusan ini diambil Yolanda di awal sesi sebelum kode ditulis.
2. Revisi: tombol download/lihat-file diganti jadi `Button` ber-ikon (sebelumnya teks underline biasa, dikira tidak bisa diklik oleh Yolanda).
3. Revisi besar -- lokasi widget dipindah keluar dari bucket page: leader dapat tombol "Budgeting Template" di kanan tiap kartu event (halaman Manajemen Kepanitiaan); panitia dapat tab baru "Submit Budget" di Workspace (urutan tab: Progres & Task Board | Tugas Saya | Timeline Pelaksanaan | Susunan Panitia | Submit Budget).
4. Revisi lanjutan -- template budgeting jadi **per event** (per `kepanitiaan`, bukan 1 file global untuk semua event), karena tiap event bisa punya kebutuhan budgeting berbeda.
5. Ditambah SEMENTARA alat bantu leader "Cek Susunan Panitia (Read-only)" di `/leader/dashboard` -- dipicu insiden akun test `panitia-e@test.local` yang tercatat di Susunan Panitia sebuah instance tapi tidak bisa login ke situ (root cause: Susunan Panitia vs hak akses login itu 2 hal berbeda). **Sudah dipakai Yolanda untuk membersihkan entri usang panitia-a/b/d/e, lalu DIHAPUS lagi dari kode** (`lib/committee/orphan-check.ts` dan `components/committee/orphan-check-section.tsx` sudah tidak ada) -- ini alat bantu sekali pakai, bukan fitur permanen, jadi jangan bingung kalau tidak ketemu filenya. Kalau nanti butuh cek serupa lagi, lihat riwayatnya di Catatan Teknis Fase 5 dan buat ulang dari situ.

**Migration yang sudah dijalankan & dikonfirmasi Yolanda**: `20260810000009_fase5_budget_storage.sql` (setelah `...0001` s/d `...0008` dari fase-fase sebelumnya).

**Yang MASIH perlu dikonfirmasi Yolanda sebelum Fase 5 dicentang selesai** -- checklist di Catatan Teknis Fase 5 (poin 9 soal alat bantu Susunan Panitia sudah tidak berlaku karena alat itu sudah dihapus, lihat poin 5 di atas), intinya: coba alur nyata download template per-event, upload submission sebagai panitia, pastikan status berubah "Lengkap" -- semua dengan tampilan FINAL (tombol di kartu event untuk leader, tab "Submit Budget" untuk panitia). Belum pernah dites eksplisit sejak lokasi widget-nya pindah beberapa kali.

**Fase 6 boleh dikerjakan lebih dulu di sesi baru, PARALEL dengan checklist Fase 5 di atas** -- ini permintaan eksplisit Yolanda, bukan penyimpangan diam-diam dari urutan "satu fase per session": Fase 6 (dashboard ringkasan lintas-instance) cuma baca kolom `budget_submissions.status` yang skemanya sudah stabil sejak migration Fase 1, tidak bergantung ke lokasi UI widget budgeting yang masih berubah-ubah. **Sesi yang mengerjakan Fase 6 tidak perlu menunggu checklist Fase 5 tuntas**, tapi checklist itu tetap harus diselesaikan (oleh Yolanda sendiri, atau sesi lain) sebelum Fase 5 resmi dicentang `[x]` di section 8 atas.

**Fase 6 -- kode sudah selesai ditulis (sesi ini), belum diverifikasi live.** Dikerjakan sesuai izin "Fase 6 boleh dikerjakan lebih dulu, PARALEL dengan checklist Fase 5" di atas -- murni membaca tabel yang sudah ada (termasuk `budget_submissions`, migration `...0009` sudah dikonfirmasi jalan), tidak ada migration baru. Detail lengkap ada di **Catatan Teknis Fase 6** di bawah.

## 9. Catatan Teknis per Fase

> Detail implementasi & keputusan teknis tiap fase, supaya session berikutnya tidak perlu menebak-nebak dari riwayat chat yang sudah tidak ada.

### Fase 0 — Setup Project

**Struktur route yang benar-benar dibuat** (route group `(auth)`/`(panitia)`/`(leader)` tidak muncul di URL, jadi path aktualnya beda dari nama foldernya):
- `app/(auth)/login/page.tsx` → `/login` (placeholder, isi sungguhan di Fase 1)
- `app/(panitia)/panitia/dashboard/page.tsx` → `/panitia/dashboard` (placeholder, Fase 2+)
- `app/(leader)/leader/dashboard/page.tsx` → `/leader/dashboard` (placeholder, Fase 6)

Catatan: awalnya kedua route group `(panitia)` dan `(leader)` sama-sama punya halaman `dashboard` langsung di dalamnya, tapi ini **collision** — route group tidak menambah segment ke URL, jadi keduanya sama-sama resolve ke `/dashboard` dan `next build` gagal. Makanya masing-masing dinested lagi satu level (`panitia/dashboard`, `leader/dashboard`). Kalau mau ubah struktur URL di fase berikutnya, ingat aturan ini.

**shadcn/ui di-setup manual**, bukan lewat `npx shadcn init` — CLI resminya butuh akses ke `ui.shadcn.com` yang diblokir network policy sandbox Claude Code on the web saat itu. Yang dibuat manual: `components.json`, `lib/utils.ts` (helper `cn()`), design tokens (CSS variables) di `app/globals.css`, dependency inti (`clsx`, `tailwind-merge`, `class-variance-authority`, `lucide-react`, `tw-animate-css`, `@radix-ui/react-slot`), dan satu komponen contoh `components/ui/button.tsx`. Kalau di session/environment lain `ui.shadcn.com` bisa diakses, silakan pakai `npx shadcn@latest add <component>` seperti biasa untuk komponen baru — strukturnya sudah kompatibel.

**Supabase client**: `lib/supabase/client.ts` (browser, pakai `createBrowserClient`) dan `lib/supabase/server.ts` (Server Component, pakai `createServerClient` + `cookies()` dari `next/headers`). Keduanya dari `@supabase/ssr`. Middleware untuk refresh session **belum dibuat** — itu bagian dari Fase 1 ("Middleware redirect berdasarkan role").

**Kredensial Supabase**: project sudah dibuat di supabase.com (Project URL: `https://hdzhlltitaxzirapkuqo.supabase.co`, key pakai format baru `sb_publishable_...`). Nilainya **hanya** ada di `.env.local` milik Yolanda secara lokal (tidak pernah di-commit, dilindungi `.gitignore` pola `.env*`). Template kosongnya ada di `.env.local.example` (ini yang di-commit). **Setiap session/environment baru yang mengerjakan project ini harus minta Yolanda isi ulang `.env.local` secara manual** — tidak bisa diasumsikan sudah ada, karena tidak tersimpan di repo maupun di sandbox cloud (sandbox bersifat ephemeral, hilang begitu session berakhir).

**Kendala network sandbox**: sandbox Claude Code on the web yang dipakai untuk Fase 0 memblokir koneksi HTTPS ke domain di luar allowlist (npm, GitHub, dst) — termasuk `*.supabase.co` dan `ui.shadcn.com`. Karena itu, verifikasi live koneksi ke Supabase **dilakukan manual oleh Yolanda di laptopnya sendiri** (clone branch, isi `.env.local`, `npm run dev`, cek tidak ada error) — bukan dari dalam sandbox. Kalau session berikutnya jalan di sandbox serupa dan butuh koneksi nyata ke Supabase (migration SQL di Fase 1, dst), kemungkinan besar akan kena kendala yang sama dan perlu strategi serupa: siapkan perintah/SQL-nya, minta Yolanda yang jalankan di sisi lokal atau lewat Supabase Dashboard (SQL Editor) langsung.

### Fase 1 — Auth & Skema Data

**Kendala network sama seperti Fase 0**: sandbox sesi ini (Claude Code remote execution environment) juga tidak punya akses ke `*.supabase.co` (dicoba `curl` ke project URL, hasilnya `403` dari proxy) maupun `ui.shadcn.com`. Jadi dari dalam sandbox, migration SQL/uji RLS/login sungguhan hanya diverifikasi tidak-langsung lewat `npm run build`, `npm run lint`, typecheck, dan smoke test `next dev` (cek redirect proxy jalan, dan server action login tidak crash walau `signInWithPassword` gagal connect).

**Verifikasi live sudah dilakukan Yolanda di laptopnya sendiri (bukan dari sandbox), hasilnya sukses:**
1. Migration `supabase/migrations/20260809000001_fase1_schema_and_rls.sql` sudah dijalankan lewat Supabase Dashboard > SQL Editor — 11 tabel + RLS policies sudah aktif di project asli.
2. Uji RLS sudah dilakukan mengikuti `supabase/verify_rls_fase1.sql` (2 akun panitia test di 2 instance berbeda + 1 akun leader) — terbukti panitia A hanya lihat data instance A, panitia B hanya instance B, leader lihat semua.
3. `.env.local` sudah diisi kredensial asli, `npm run dev` dijalankan, login dengan akun test berhasil dan redirect ke `/panitia/dashboard` maupun `/leader/dashboard` sesuai role masing-masing.

Data test (`*@test.local`) sudah dibersihkan Yolanda dari database setelah verifikasi selesai — jadi kalau di Fase 2 nanti tabel `sites`/`kepanitiaan`/`kepanitiaan_site`/`users` masih kosong, itu wajar, bukan bug.

**`middleware.ts` → `proxy.ts`**: Next.js 16 men-deprecate file convention `middleware.js` dan menggantinya dengan `proxy.js` (fungsi dan nama file berubah, behavior sama — lihat `node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/proxy.md`). Karena itu redirect-berdasarkan-role dan refresh session Supabase ditaruh di **`proxy.ts`** di root project (bukan `middleware.ts`), dengan fungsi bernama `proxy` (bukan `middleware`). Logic session-refresh-nya sendiri ada di helper `lib/supabase/middleware.ts` (nama file helper ini sengaja dipertahankan, karena bukan file-convention Next.js — cuma modul biasa; kalau mau di-rename ke `lib/supabase/proxy.ts` boleh, tidak ada bedanya secara fungsional). `proxy.ts` meng-exclude `_next/static`, `_next/image`, `favicon.ico`, dan file gambar statis dari matcher-nya.

**Auth flow**: pakai Next.js Server Actions (React 19 `useActionState`), bukan client-side `fetch` ke Supabase langsung — mengikuti pola resmi Supabase+Next.js App Router.
- `lib/auth/actions.ts`: `login(prevState, formData)` — panggil `supabase.auth.signInWithPassword`, lalu query role dari `public.users`, lalu `redirect()` ke `/panitia/dashboard` atau `/leader/dashboard`. Kalau gagal (password salah / user belum ada di `public.users`), return `{ error: "..." }` (tidak melempar exception, supaya `useActionState` di client bisa nampilin pesan error). `logout()` — `supabase.auth.signOut()` lalu redirect ke `/login`.
- `app/(auth)/login/login-form.tsx` (client component) memanggil `login` lewat `useActionState`, dan `app/(auth)/login/page.tsx` membungkusnya dengan `Card` dari shadcn/ui.
- `components/logout-button.tsx`: form kecil yang langsung `action={logout}` (server action), dipakai di kedua dashboard placeholder biar mudah dites bolak-balik role tanpa buka tab incognito baru terus.

**Role redirect di `proxy.ts`**: setiap request (kecuali asset statis) memanggil `supabase.auth.getUser()` (bukan `getSession()` — `getUser()` yang mem-verifikasi & refresh token server-side, sesuai rekomendasi Supabase). Kalau belum login dan akses `/panitia/*`, `/leader/*`, atau `/` → redirect ke `/login`. Kalau sudah login: akses `/login` atau `/` → redirect ke dashboard sesuai role; panitia yang coba akses `/leader/*` atau leader yang coba akses `/panitia/*` → di-redirect balik ke dashboard sendiri (bukan 403, biar UX-nya nggak nyangkut di halaman error).

**Skema data & RLS** (`supabase/migrations/20260809000001_fase1_schema_and_rls.sql`):
- Semua 10 tabel di section 5 dibuat sesuai model data, dengan tambahan `created_at` di tiap tabel dan check constraint untuk kolom status/role yang disebutkan eksplisit di handover (`users.role`, `committee_members.role`, `tasks.status`/`subtasks.status` pakai `belum`/`proses`/`selesai`, `budget_submissions.status` pakai `belum`/`lengkap` sesuai istilah di section Fase 5). `notifications_log.jenis` sengaja dibiarkan `text` bebas (belum di-constrain) karena daftar jenis notifikasi belum diputuskan final — itu keputusan Fase 7.
- `public.users.id` adalah FK langsung ke `auth.users.id` (satu baris `public.users` = satu akun Supabase Auth). **Tidak ada trigger auto-insert dari `auth.users` ke `public.users`** — akun panitia/leader sengaja dibuat manual oleh leader (lewat Supabase Dashboard atau Fase 2 nanti), jadi `role` dan `kepanitiaan_site_id` selalu diisi sadar, bukan default kosong.
- RLS pakai 3 helper function `security definer` (`current_user_role()`, `current_user_kepanitiaan_site_id()`, `is_leader()`) yang baca `public.users` tanpa kena RLS-nya sendiri — ini untuk menghindari infinite recursion kalau policy tabel `users` butuh cek role dari tabel `users` itu sendiri. Semua tabel operasional pakai pola `is_leader() OR kepanitiaan_site_id = current_user_kepanitiaan_site_id()`; untuk `tasks`/`subtasks` yang tidak punya `kepanitiaan_site_id` langsung, resolve lewat join ke `buckets` (dan `tasks` untuk `subtasks`).
- Bucket seeding otomatis (6 bucket default saat `kepanitiaan_site` baru dibuat) **belum dibuat di migration ini** — itu memang scope Fase 2 ("Manajemen Kepanitiaan"), bukan Fase 1 ("Migration SQL + RLS" saja). Kalau di Fase 2 mau pakai DB trigger untuk auto-seed, tinggal tambah migration baru, jangan ubah file ini.

**shadcn/ui component baru**: `input.tsx`, `label.tsx` (butuh `@radix-ui/react-label`, sudah ditambahkan ke `package.json`), `card.tsx` — dibuat manual dengan pola yang sama seperti `button.tsx` di Fase 0 (network ke `ui.shadcn.com` masih diblokir, sudah dicoba `npx shadcn@latest add` dan gagal dengan error yang sama seperti di Fase 0).

**`app/page.tsx`**: konten boilerplate `create-next-app` diganti jadi `redirect("/login")` polos — karena `proxy.ts` sudah selalu redirect `/` ke `/login` atau dashboard sesuai role duluan, halaman ini praktis cuma fallback kalau proxy ter-skip (harusnya tidak pernah kejadian secara normal).

### Fase 2 — Manajemen Kepanitiaan & Susunan Panitia

**`npm install` dijalankan di sandbox** (baru pertama kali — `node_modules` belum pernah ada sebelumnya di sessions Fase 0/1). Ternyata registry npm memang tidak diblokir (sesuai catatan Fase 0/1: yang diblokir cuma domain di luar allowlist seperti `*.supabase.co` dan `ui.shadcn.com`), jadi dependency ter-install normal dan `next build`/`next lint`/`tsc --noEmit` bisa dijalankan sungguhan di sandbox ini (bukan cuma dibaca kodenya). `.env.local` diisi nilai placeholder sementara hanya untuk keperluan `next build` di sandbox (supaya module `lib/supabase/*` tidak crash saat baca `process.env`), lalu dihapus lagi sebelum commit — **tidak pernah** commit ke git (tetap terlindungi `.gitignore` pola `.env*`). Koneksi nyata ke Supabase tetap tidak bisa dites dari sandbox ini (belum dicoba ulang, tapi tidak ada indikasi allowlist `*.supabase.co` berubah), jadi migration & RLS baru tetap perlu dijalankan manual oleh Yolanda seperti Fase 1.

**Migration baru**: `supabase/migrations/20260809000002_fase2_kepanitiaan_dan_seed_bucket.sql` (dijalankan setelah migration Fase 1). Isinya dua bagian:
1. Fungsi `public.seed_default_buckets()` + trigger `trg_seed_default_buckets` (`after insert on kepanitiaan_site`) yang otomatis insert 6 baris `buckets` (nama & `is_budgeting` sesuai section 5 HANDOVER.md) setiap kali instance `kepanitiaan_site` baru dibuat. Sengaja **bukan** `security definer` — trigger jalan dengan privilege pemanggil (leader), dan policy `buckets_scoped` Fase 1 sudah mengizinkan leader insert lewat `is_leader()`, jadi tidak perlu bypass RLS.
2. Fungsi RPC `public.create_kepanitiaan_dengan_sites(p_nama text, p_nama_sites text[])`: insert 1 baris `kepanitiaan`, lalu untuk setiap nama site di array — find-or-create ke tabel `sites` by `nama_site` (karena tabel `sites` belum punya halaman CRUD terpisah, lihat poin di bawah), lalu insert `kepanitiaan_site` (`on conflict do nothing` untuk idempotensi). Juga **bukan** `security definer`, jadi RLS (`kepanitiaan_write_leader`, `sites_write_leader`, `kepanitiaan_site_write_leader`) tetap berlaku otomatis berdasarkan role pemanggil — panggilan dari akun panitia akan gagal dengan error RLS, bukan lewat pengecekan manual di kode. Ditambahkan `grant execute ... to authenticated` supaya bisa dipanggil lewat `supabase.rpc()` dari sisi aplikasi.

**Keputusan: tidak ada halaman CRUD terpisah untuk tabel `sites`.** HANDOVER.md section 5-6 tidak menugaskan pengelolaan `sites` ke fase manapun secara eksplisit, tapi form "Buat Kepanitiaan Baru" butuh sumber data site. Solusi minimal yang dipilih: form punya checkbox untuk site yang sudah ada (query `select * from sites`) **dan** input teks "tambah site baru" (dipisah koma/baris baru) yang di-find-or-create otomatis lewat RPC di atas. Ini bukan fitur baru di luar scope — cuma cara mengisi tabel `sites` yang sudah ada di model data Fase 1 — tapi dicatat di sini supaya jelas kalau nanti section 5/6 diperluas dengan halaman manajemen site sendiri, keputusan ini harus direvisit.

**Struktur route baru**:
- `/leader/kepanitiaan` — daftar kepanitiaan (dikelompokkan per nama), tiap kepanitiaan menampilkan tombol per instance site-nya. Tombol "+ Buat Kepanitiaan Baru".
- `/leader/kepanitiaan/baru` — form buat kepanitiaan (client component `create-kepanitiaan-form.tsx` pakai `useActionState`, mengikuti pola `login-form.tsx` Fase 1).
- `/leader/kepanitiaan/[instanceId]` — detail satu instance `kepanitiaan_site`: daftar bucket (read-only, cuma untuk membuktikan auto-seed jalan — CRUD tugas per bucket itu scope Fase 4) + CRUD susunan panitia.
- `/panitia/susunan` — halaman susunan panitia milik akun panitia sendiri, `kepanitiaan_site_id` di-resolve dari `public.users` di server (bukan dari input), supaya panitia tidak bisa lihat/edit instance lain (RLS `committee_members_scoped` juga menahan ini sebagai lapis kedua).

Kedua halaman susunan panitia (leader & panitia) memakai komponen bersama `components/committee/committee-members-section.tsx` (Server Component, bukan client) — tiap baris anggota adalah `<form>` sendiri (bukan toggle edit-mode) yang langsung menampilkan input ter-isi nilai sekarang + tombol "Simpan" dan `<form>` "Hapus" di sampingnya, jadi keseluruhan CRUD **tidak butuh client-side JS sama sekali** kecuali form create-kepanitiaan (yang butuh `useActionState` untuk pesan error).

**Server actions**: `lib/kepanitiaan/actions.ts` (`createKepanitiaan`, panggil RPC di atas) dan `lib/committee/actions.ts` (`addCommitteeMember`/`updateCommitteeMember`/`deleteCommitteeMember`). Argumen seperti `kepanitiaanSiteId`/`memberId`/`redirectTo` di-pass lewat `.bind(null, ...)` dari Server Component (bukan hidden input) — sesuai `node_modules/next/dist/docs/01-app/02-guides/forms.md`, argumen yang di-bind ke Server Action terenkripsi di closure dan tidak bisa ditempel/diubah dari sisi client, jadi ini lebih aman daripada hidden input untuk kasus di mana panitia seharusnya tidak bisa mengubah `kepanitiaan_site_id` tujuan insert. Error ditampilkan lewat redirect ke `?error=...` di URL yang sama (dibaca lewat `searchParams`), bukan `useActionState`, karena form-form ini Server Component murni.

**Role anggota panitia** (`committee_members.role`) dipilih lewat elemen `<select>` HTML biasa (bergaya mengikuti `input.tsx`), bukan komponen shadcn `Select` — cuma 2 pilihan tetap (`anggota`/`leader_bidang`), jadi membuat komponen Radix Select baru (butuh dependency `@radix-ui/react-select` baru) dianggap berlebihan untuk kebutuhan ini. Kalau butuh dropdown custom-styled di fase lain, ikuti pola manual `label.tsx` untuk bikin `components/ui/select.tsx`.

**Migration lanjutan `20260809000003_fase2_tambah_site_ke_kepanitiaan_existing.sql`** (dibuat saat verifikasi live, `create or replace` atas RPC di migration `...0002`): versi pertama RPC selalu `insert` baris baru ke `kepanitiaan`, jadi submit nama yang sudah terpakai langsung kena unique constraint dan gagal dengan "Nama kepanitiaan ini sudah dipakai". Efek praktisnya fatal — satu-satunya cara menambah site ke kepanitiaan yang sudah berjalan (misal FIND yang tadinya 2 site lalu buka di Jakarta-Area) adalah hapus + bikin ulang, yang ikut menghapus susunan panitia & bucket yang sudah diisi. Diperbaiki jadi find-or-create untuk `kepanitiaan`, sama persis dengan pola yang sudah dipakai untuk `sites` di function yang sama; insert ke `kepanitiaan_site` tetap `on conflict do nothing`, jadi submit ulang kombinasi kepanitiaan+site yang sudah ada = no-op (tidak bikin instance ganda maupun bucket dobel). Konsekuensi yang disadari: proteksi duplikat nama hilang, jadi typo nama (misal "FIND " vs "FIND") bisa diam-diam menambah site ke kepanitiaan yang salah — `trim()` sudah dipasang untuk kasus spasi, tapi kalau nanti daftar kepanitiaan sudah banyak, pertimbangkan ganti input teks bebas jadi dropdown "pilih kepanitiaan yang sudah ada / buat baru". Halaman & tombolnya ikut diganti jadi "Buat Kepanitiaan / Tambah Site" supaya perilaku ini jelas ke leader.

**Rename & delete kepanitiaan/instance** (diminta Yolanda saat verifikasi live, tidak ada di rincian section 6 tapi masih di dalam judul fase "Manajemen Kepanitiaan"): di `/leader/kepanitiaan` tiap card punya input nama yang bisa diedit + tombol "Simpan Nama", dan tombol "Hapus Kepanitiaan" (menghapus kepanitiaan beserta semua instance site-nya lewat `on delete cascade`). Di halaman detail instance ada tombol "Hapus Instance Ini" yang hanya menghapus satu kombinasi kepanitiaan+site. Pemicunya: waktu verifikasi, satu-satunya cara menghapus kepanitiaan adalah `delete from` manual di SQL Editor — tidak masuk akal untuk dipakai leader sehari-hari.
- Aksi hapus pakai `components/confirm-submit-button.tsx` (client component tipis, `window.confirm()` lalu `preventDefault()` kalau dibatalkan). Kalau JS mati tombolnya tetap jadi submit biasa, jadi form-nya tidak rusak.
- Sebelum menghapus, action mengecek dulu jumlah akun di `public.users` yang `kepanitiaan_site_id`-nya menunjuk ke instance yang mau dihapus. Kalau masih ada, hapus **ditolak** dengan pesan yang jelas, bukan dilempar sebagai error database. Alasannya: FK `users.kepanitiaan_site_id` itu `on delete set null`, tapi check constraint `panitia_harus_punya_instance` melarang panitia punya `kepanitiaan_site_id` null — jadi delete-nya pasti gagal di level DB dengan pesan yang tidak bisa dibaca leader. Menghapus akun panitia secara otomatis sengaja **tidak** dilakukan (akun auth itu milik orang sungguhan; keputusan hapus/pindah akun harus disengaja, bukan efek samping hapus kepanitiaan).

**Verifikasi live sudah dilakukan Yolanda (bukan dari sandbox), hasilnya sukses:** kedua migration Fase 2 sudah dijalankan lewat Supabase Dashboard > SQL Editor, form buat kepanitiaan berhasil membuat instance baru, tiap instance otomatis dapat 6 bucket default (termasuk Budgeting bertanda `is_budgeting`), dan CRUD susunan panitia (tambah anggota) berjalan normal.

**Kendala yang muncul saat verifikasi live** (dicatat supaya fase berikutnya tidak mengulang):
- **`delete from public.users where email like '%@test.local'` mengunci akun sendiri.** Perintah ini dipakai untuk membersihkan user test yang menghalangi `delete from kepanitiaan` (constraint `panitia_harus_punya_instance` menolak `on delete set null` selama masih ada panitia yang nempel ke instance itu), tapi ikut menghapus `leader@test.local` yang sedang dipakai login. Akibatnya login jadi gagal dengan pesan "Akun ini belum terdaftar di sistem orientasi panitia" — baris `auth.users` masih ada (password tetap diterima) tapi baris `public.users`-nya hilang. Pulihkan dengan `insert into public.users (id, email, role, kepanitiaan_site_id) select id, email, 'leader', null from auth.users where email = '<email>' on conflict (id) do update set role = 'leader', kepanitiaan_site_id = null;`. Pelajaran: hapus user test dengan filter yang spesifik ke instance yang mau dibuang, jangan sapu semua `@test.local`.
- **`rm -rf` tidak jalan di Windows PowerShell/CMD**, dan `npm` di PowerShell bisa kena `running scripts is disabled on this system` (execution policy laptop kantor) — pakai **Command Prompt**, bukan PowerShell. Untuk hapus cache build, hapus folder `.next` manual lewat File Explorer.
- **Server dev lama sering nyangkut di port 3000** setelah beberapa kali restart, bikin halaman lambat/`Failed to fetch` walau kodenya sudah benar. Next.js akan bilang "Another next dev server is already running" + kasih PID-nya; matikan dengan `taskkill /PID <pid> /F` lalu `npm run dev` lagi.

**Belum diverifikasi live**: RLS `committee_members_scoped` belum diuji ulang khusus untuk flow tambah/edit/hapus anggota dari dua akun panitia berbeda (Fase 1 hanya menguji isolasi di tabel `buckets`), dan halaman `/panitia/susunan` belum pernah dibuka dengan akun panitia sungguhan karena akun test-nya sudah dihapus. Langkah manualnya ada di `supabase/verify_fase2.sql` poin 7-8 — layak dikerjakan di awal Fase 3 kalau nanti bikin akun panitia baru.

### Fase 3 — Timeline Pelaksanaan

**Tidak ada migration SQL baru di fase ini.** Tabel `timeline_milestones` dan policy `timeline_milestones_scoped` (pola `is_leader() OR kepanitiaan_site_id = current_user_kepanitiaan_site_id()`, sama seperti `buckets`/`committee_members`) sudah dibuat dari migration Fase 1 (`20260809000001_fase1_schema_and_rls.sql`) tapi belum pernah dipakai UI-nya sampai fase ini. Karena RLS-nya sudah ada dan sudah mengikuti pola yang sama-sama dipakai `buckets` (yang sudah teruji isolasinya di Fase 1), Fase 3 murni kerja UI — tidak menyentuh skema/RLS sama sekali.

**Komponen & actions baru**, mengikuti pola `committee-members-section.tsx`/`lib/committee/actions.ts` dari Fase 2 persis:
- `lib/timeline/actions.ts`: `addMilestone`/`updateMilestone`/`deleteMilestone`. Validasi tambahan yang tidak ada di pola committee: `tanggal_selesai` (opsional) ditolak dengan pesan error kalau lebih awal dari `tanggal_mulai` (perbandingan string ISO `yyyy-mm-dd`, aman karena format `<input type="date">` selalu ISO).
- `components/timeline/timeline-section.tsx` (`TimelineSection`, dipakai leader & panitia): render "vertical timeline sederhana" — list milestone diurutkan ascending by `tanggal_mulai` (di-sort di komponen, bukan query, supaya konsisten walau data belum di-`order()` dengan benar dari pemanggil), tiap item punya dot + garis vertikal di kolom kiri (garis di-skip untuk item terakhir) dan card form edit di kanan (pola form-per-baris yang sama seperti committee members: tidak butuh client-side JS). Form tambah milestone baru di bagian bawah, sama seperti bucket committee.

**Halaman**:
- Leader: **tidak dibuat route baru** — `TimelineSection` di-inline langsung ke `/leader/kepanitiaan/[instanceId]/page.tsx` (di atas `CommitteeMembersSection`), mengikuti pola halaman detail instance Fase 2 yang sudah menumpuk beberapa section (bucket read-only + committee) di satu halaman. Kalau nanti halaman ini kepanjangan setelah Fase 4/5 nambah section lagi, pertimbangkan pecah jadi tab/sub-route.
- Panitia: dibuat route baru `/panitia/timeline`, isinya sama persis strukturnya dengan `/panitia/susunan` (resolve `kepanitiaan_site_id` dari `public.users` di server, bukan dari input, supaya panitia tidak bisa akses instance lain — RLS `timeline_milestones_scoped` jadi lapis kedua). Link ke halaman ini ditambahkan di `/panitia/dashboard`.

**Verifikasi yang sudah dilakukan (di sandbox, bukan live)**: `npm install` (registry npm tidak diblokir, sama seperti Fase 2), `next build`, `next lint`, `tsc --noEmit` — semua sukses dengan `.env.local` placeholder sementara (dihapus lagi sebelum commit, tidak pernah masuk git, sama seperti pola Fase 2). Koneksi nyata ke Supabase tetap tidak bisa dites dari sandbox ini.

**Status verifikasi live**: Yolanda sudah pakai halaman Timeline secara langsung (tambah milestone, coba tab Vertikal & Mingguan, lihat badge status, konfirmasi Gantt chart tampil benar setelah 2 ronde bugfix). **Independensi timeline antar-instance sudah dikonfirmasi** — dicoba dengan instance nyata ("Habiha @ Ciawi" dkk), milestone tidak tercampur antar-instance. Ini kriteria "Selesai jika" resmi Fase 3 di section 6, sudah terpenuhi.

**Isolasi RLS dari sisi akun panitia juga sudah dikonfirmasi**, setelah sempat kena kendala akun test terlebih dahulu (dicatat di sini karena kemungkinan besar akan kejadian lagi kalau butuh akun test serupa di fase-fase berikutnya):

- Akun `panitia-a@test.local`/`panitia-b@test.local` (dibuat untuk verifikasi RLS Fase 1, lihat `verify_rls_fase1.sql`) sempat **ditolak login** — bukan bug kode. Root cause: HANDOVER Fase 1 mencatat data test `*@test.local` dibersihkan Yolanda setelah verifikasi Fase 1 selesai, jadi baris `public.users` untuk kedua akun ini (dan instance test lama yang jadi rujukan `kepanitiaan_site_id`-nya) sudah ikut terhapus — sementara baris `auth.users`-nya (akun login-nya sendiri) ternyata masih ada. Gejalanya: Supabase Auth berhasil, tapi `lib/auth/actions.ts` `login()` menolak dengan pesan "Akun ini belum terdaftar di sistem orientasi panitia" karena tidak nemu baris `public.users`-nya.
- Diperbaiki dengan `supabase/verify_fase3.sql` (dibuat di revisi ini, pola sama seperti `verify_rls_fase1.sql`/`verify_fase2.sql`): upsert baris `public.users` untuk kedua akun itu, disambungkan ke 2 instance ASLI yang sekarang ada (bukan instance test lama yang sudah hilang).
- Setelah disambungkan ulang: login kedua akun sukses, masing-masing cuma bisa lihat & modifikasi instance-nya sendiri (dicoba di `/panitia/susunan` dan halaman Timeline) — isolasi RLS `timeline_milestones_scoped` **dan** `committee_members_scoped` dari sisi panitia terbukti bekerja. Ini juga sekaligus menutup celah verifikasi `committee_members` dari Fase 2 (`verify_fase2.sql` poin 7-8) yang belum pernah dicoba sebelumnya.

**Revisi setelah review Yolanda** (4 permintaan, dikerjakan di sesi yang sama, sebelum verifikasi live pertama sempat dilakukan — jadi poin 1-4 di atas masih berlaku dan sekarang meliputi UI hasil revisi juga):

1. **Flag status milestone** (`lewat`/`berlangsung`/`akan_datang`) — dihitung murni di UI, tidak ada kolom status baru di `timeline_milestones` (statusnya derived, bukan disimpan). Helper di `lib/timeline/format.ts`: `getMilestoneStatus(tanggal_mulai, tanggal_selesai, todayIso)` bandingkan string ISO `yyyy-mm-dd` (aman secara leksikal). "Hari ini" dihitung lewat `todayJakartaISO()` yang mem-fix timezone ke `Asia/Jakarta` (bukan ikut timezone server yang defaultnya UTC di Vercel) — supaya status tidak salah geser di dini hari WIB. Badge-nya pakai komponen baru `components/ui/badge.tsx` (dibuat manual, pola sama seperti `button.tsx`/`card.tsx`, karena `ui.shadcn.com` masih terblokir) dengan varian `outline`/`default`/`muted` yang dipetakan ke 3 status lewat `STATUS_BADGE_VARIANT`/`STATUS_LABEL` — sengaja masih pakai token warna netral yang sudah ada (belum ada warna hijau/biru khusus di `globals.css`), biar tidak mendahului polish warna yang jadi scope Fase 8.
2. **Tampilan Mingguan** — toggle tab "Vertikal"/"Mingguan" di atas daftar milestone (tidak menghilangkan tampilan vertikal, cuma tambahan). **Revisi kedua: ini sekarang Gantt chart**, bukan list-per-minggu (opsi list-per-minggu adalah pilihan pertama Yolanda dari preview yang diajukan, tapi diganti lagi ke gaya grid/visual di revisi berikutnya begitu sudah dicoba live — jadi opsi list-per-minggu **sudah tidak dipakai lagi**, kalau lihat referensi lama di riwayat chat/kode jangan bingung). Implementasi Gantt di `components/timeline/timeline-gantt.tsx`: CSS Grid manual (bukan library chart, karena project ini tidak punya dependency chart apapun) — kolom = minggu (dari minggu paling awal sampai minggu paling akhir di antara semua milestone, dihitung lewat `buildGanttWeeks()` di `lib/timeline/format.ts`), baris = satu milestone per baris, tiap baris punya label nama di kolom pertama + "bar" berwarna yang di-span dari kolom minggu mulai sampai kolom minggu selesai (posisi bar diatur manual lewat inline style `gridRow`/`gridColumn`, **bukan** auto-placement CSS Grid, supaya tidak tergantung urutan render). Warna bar ikut status (`akan_datang`/`berlangsung`/`lewat`) pakai token warna netral yang sama seperti badge, plus ada legend kecil di bawah chart. **Gantt ini read-only** — tidak ada Edit/Hapus di tampilan ini, edit tetap lewat tab Vertikal. Chart-nya dibungkus `overflow-x-auto` supaya tidak merusak layout kalau minggunya banyak (scroll horizontal di dalam card, bukan di halaman).
3. **Milestone tersimpan di-lock, klik Edit untuk ubah** — ini yang bikin section timeline butuh client-side JS (beda dari susunan panitia yang sengaja tanpa JS): `components/timeline/milestone-row.tsx` (`"use client"`) simpan state `isEditing` lokal. Mode tampilan (default): nama + ikon gembok (`Lock` dari `lucide-react`, sudah jadi dependency dari Fase 0) + badge status + rentang tanggal sebagai teks biasa (tidak ada `<input>` sama sekali, jadi tanggal benar-benar tidak bisa diketik langsung) + tombol "Edit"/"Hapus". Mode edit: form biasa (persis versi lama) + tombol "Simpan"/"Batal". "Simpan" memanggil server action `updateAction` **langsung sebagai function call** dari `action={handleSave}` (bukan cuma taruh referensi server action di `action=` form attribute), lalu `setIsEditing(false)` sesudahnya — pola ini (server action dipanggil imperatif dari client, error tetap lempar lewat `redirect()` yang otomatis di-handle Next.js sebagai client-side navigation) belum pernah dipakai di codebase ini sebelumnya, jadi kalau ada pola serupa dibutuhkan di fase lain (misal Fase 4 kalau butuh perilaku sama di CRUD tugas), ikuti contoh di sini. Ini cuma berlaku di tab Vertikal — tab Mingguan (Gantt) read-only, lihat poin 2.
   - Struktur jadi 4 lapis: `TimelineSection` (server, fetch & bind action, render Card + form tambah) → `TimelineBody` (`"use client"`, pegang state tab Vertikal/Mingguan) → `MilestoneRow` (`"use client"`, pegang state edit per baris, dipakai tab Vertikal) / `TimelineGantt` (dipakai tab Mingguan, tidak butuh state sendiri). Kalau butuh nambah kolom milestone baru, edit di 3 tempat minimal: `Milestone` type (`timeline-section.tsx`), form field di `MilestoneRow`, dan kolom di query Supabase pemanggil halaman — plus `TimelineGantt`/`buildGanttWeeks` kalau kolom barunya relevan ditampilkan di Gantt juga.
4. **Leader Bidang bisa pilih bidang** — field baru `committee_members.bucket_id` (nullable, FK ke `buckets.id`, `on delete set null`), ditambah lewat migration baru `20260809000004_fase3_committee_bucket_assignment.sql` (bukan bagian dari migration Fase 1, karena kolom ini memang belum ada saat itu). Selain kolom, migration ini juga nambah trigger `trg_check_committee_member_bucket_instance` yang memvalidasi `bucket_id` yang dipilih harus milik `kepanitiaan_site_id` yang sama dengan baris `committee_members`-nya — defense-in-depth di level DB, karena UI cuma menawarkan pilihan bucket dari instance yang benar tapi tidak ada yang mencegah request manual langsung ke Supabase REST API mengisi `bucket_id` dari instance lain.
   - Dropdown "Bidang" **selalu ditampilkan di form** (kecuali kalau instance-nya kebetulan belum punya bucket sama sekali — dijaga lewat `buckets.length > 0`, walau praktiknya harusnya selalu ada karena 6 bucket default auto-seed), terlepas dari `role` yang dipilih — mengikuti prinsip section susunan panitia yang sengaja tidak butuh client-side JS. **Revisi kedua: opsi placeholder "- (bukan leader bidang)" dihapus** (permintaan Yolanda: sudah ada field Role yang membedakan anggota/leader_bidang, jadi dropdown Bidang tidak perlu punya cabang "kosong"-nya sendiri) — sekarang dropdown-nya selalu default ke bucket yang tersimpan (`member.bucket_id`) atau bucket pertama (`buckets[0].id`) kalau belum ada yang tersimpan. Validasinya tetap di server, bukan di tampilan: `lib/committee/actions.ts` punya helper `parseBucketId(role, value)` yang **selalu me-null-kan** `bucket_id` kalau `role !== "leader_bidang"` — jadi meskipun dropdown menunjukkan sebuah bucket terpilih untuk anggota biasa, nilai itu diabaikan sepenuhnya saat disimpan (murni kosmetik/tidak berarti apa-apa untuk role anggota), dan tidak ada `bucket_id` yang "nyangkut" kalau seseorang di-downgrade dari leader_bidang ke anggota.
   - Komponen `CommitteeMembersSection` sekarang butuh prop baru `buckets: { id, nama_bidang }[]` — kedua pemanggilnya (`/leader/kepanitiaan/[instanceId]/page.tsx` dan `/panitia/susunan/page.tsx`) sudah diupdate untuk fetch & pass ini (halaman panitia sebelumnya belum pernah query tabel `buckets` sama sekali, jadi ini query baru di halaman itu).
   - **Belum ada UI yang menampilkan "bidang siapa" di luar form CRUD ini** (misal daftar ringkas "Bidang Acara: Budi (leader), Sari (anggota)") — kalau nanti dibutuhkan, itu kemungkinan lebih pas ditaruh di halaman bucket Fase 4 (yang sudah punya konteks per-bidang), bukan di section susunan panitia ini.

**Tambahan langkah verifikasi live** untuk revisi di atas (di luar 4 poin sebelumnya):
5. Jalankan migration baru `20260809000004_fase3_committee_bucket_assignment.sql` lewat Supabase Dashboard > SQL Editor (setelah migration `...0001`-`...0003`).
6. Di form tambah/edit anggota panitia, pilih role "Leader Bidang" + pilih salah satu bidang di dropdown "Bidang" — simpan, refresh halaman, pastikan pilihan bidangnya tetap tersimpan (dropdown menunjukkan bucket yang benar sebagai default).
7. Ubah role anggota yang sudah punya bidang balik ke "Anggota" — simpan — pastikan `bucket_id`-nya ikut hilang (buka lagi form-nya, seharusnya `bucket_id` di database jadi `null` walau dropdown-nya tampil menunjukkan salah satu bucket — ini kosmetik yang disengaja, lihat poin 4 di atas).
8. Di card Timeline, klik tab "Vertikal": coba "Edit" pada satu milestone, ubah tanggal, "Simpan" — pastikan baris otomatis balik ke mode terkunci (bukan tetap dalam mode form). Cek juga badge status berubah otomatis sesuai tanggal hari ini (misal buat milestone dengan `tanggal_mulai` besok vs hari ini vs kemarin, badgenya harus beda: "Akan Datang"/"Berlangsung"/"Sudah Lewat").
9. Klik tab "Mingguan" — pastikan muncul Gantt chart: nama milestone di kiri, bar berwarna sesuai statusnya di kanan, span kolom minggunya sesuai `tanggal_mulai`-`tanggal_selesai`. Coba dengan milestone yang rentangnya beberapa minggu (misal 3 minggu) — bar-nya harus nyambung memanjang, bukan putus-putus. Kalau ada banyak milestone dengan tanggal yang jauh terpisah (misal satu di Agustus, satu di Desember), cek juga chart-nya tetap bisa di-scroll ke kanan (tidak merusak layout halaman).

**Revisi ketiga — hapus bucket "Penetapan Susunan Kepanitiaan" dari default seed** (bukan bucket/bidang, harusnya jadi tugas di dalam bucket lain — keputusan konten, bukan bug): migration baru `20260809000005_fase3_hapus_bucket_penetapan_susunan.sql` mengubah `seed_default_buckets()` (`create or replace`, definisi lama di migration `...0002` jadi usang tapi filenya sengaja tidak diedit — konsisten dengan pola "jangan ubah migration lama, tambah migration baru" yang sudah dipakai di Fase 2 untuk RPC serupa) supaya instance baru ke depan cuma dapat 5 bucket default (lihat section 5 yang sudah diupdate). Migration ini juga langsung `delete` baris bucket "Penetapan Susunan Kepanitiaan" yang sudah ke-seed di instance-instance yang sudah ada (idempotent, aman dijalankan berkali-kali). Efek sampingnya otomatis lewat FK yang sudah ada dari Fase 1/3: `committee_members.bucket_id` yang kebetulan menunjuk bucket ini jadi `null` (on delete set null), dan task/subtask di dalamnya (kemungkinan besar belum ada karena Fase 4 belum berjalan) ikut terhapus (on delete cascade). **Tidak ada perubahan kode aplikasi** untuk revisi ini — nama bucket tidak pernah di-hardcode di UI, cuma ditampilkan apa adanya dari hasil query, jadi cukup jalankan migration-nya saja.

**Tambahan langkah verifikasi**:
10. Jalankan migration `20260809000005_fase3_hapus_bucket_penetapan_susunan.sql` lewat Supabase Dashboard > SQL Editor.
11. Buka instance yang sudah ada (misal FIND@Cibitung) — card "Bucket Kepanitiaan" harus tinggal 5 bucket, tidak ada lagi "Penetapan Susunan Kepanitiaan".
12. Buat kepanitiaan baru (atau tambah site baru ke kepanitiaan yang sudah ada) — instance barunya harus langsung ter-seed 5 bucket, bukan 6.

**Bugfix — posisi bar di Gantt chart meleset** (dilaporkan Yolanda dari screenshot live: jarak antar label "Minggu 1/2/3" cuma 6 hari bukan 7, dan bar milestone yang lebih belakang di daftar tampil menimpa kolom nama-nya, bukan di kolom minggu yang benar). Root cause: `getWeekStart`/`addDays` di `lib/timeline/format.ts` memakai `date.toISOString().slice(0, 10)` untuk serialisasi tanggal — `toISOString()` mengonversi ke UTC dulu, jadi untuk timezone di depan UTC (WIB/WITA/WIT semuanya begitu) tanggalnya mundur 1 hari. Karena `buildGanttWeeks()` memanggil `addDays()` berantai (tiap kolom minggu dihitung dari kolom sebelumnya), error mundur-1-hari itu **menumpuk tiap iterasi** — kolom minggu ke-2 meleset 1 hari, ke-3 meleset 2 hari, dst — sampai akhirnya `findIndex` di `timeline-gantt.tsx` gagal mencocokkan minggu milestone yang lebih belakang dengan kolom yang benar (balik ke -1, yang bikin bar-nya dianggap mulai dari kolom 1 alias kolom nama, menimpa teks nama milestone-nya). Diperbaiki dengan helper baru `toLocalIso()` yang membangun string `yyyy-mm-dd` dari `getFullYear()`/`getMonth()`/`getDate()` (tetap di local time, tidak pernah round-trip ke UTC) — dipakai `getWeekStart` dan `addDays`. Tidak ada perubahan lain (tidak ada migration, tidak ada perubahan komponen) — murni perbaikan 1 fungsi helper. Sudah dicek manual di sandbox lewat simulasi Node dengan `TZ=Asia/Jakarta` yang mereproduksi persis tanggal buggy dari screenshot (`26 Jul`/`1 Agu`/`7 Agu`) sebelum fix, dan kelipatan 7 hari yang benar (`27 Jul`/`3 Agu`/`10 Agu`/...) sesudah fix. **Sudah dikonfirmasi live oleh Yolanda** — Gantt chart tampil benar setelah fix ini.

**Bugfix kedua — bar Gantt sempit/tidak sejajar kolom, dan warna "Akan Datang"/"Sudah Lewat" identik** (dilaporkan dari screenshot instance dengan cuma 1 minggu total: bar-nya tampil sebagai pil kecil di sisi kiri, bukan mengisi lebar kolom "Minggu 1"). Dua penyebab berbeda di file yang sama (`components/timeline/timeline-gantt.tsx`):
- **Root cause posisi**: grid container-nya pakai class `w-max` (`width: max-content`). Ini bikin grid selalu menyusut ke lebar minimum kontennya, sehingga kolom minggu yang seharusnya melebar mengisi sisa ruang kosong (lewat `1fr` di `gridTemplateColumns`) malah ikut menyusut ke lebar minimum (`72px`) — makanya bar-nya kelihatan sempit dan tidak proporsional dengan lebar card. Diganti jadi `min-w-full` (`min-width: 100%`): kalau kolom-kolomnya cukup sedikit, grid tetap mengisi penuh lebar card seperti seharusnya (dan `1fr` benar-benar melebar); kalau kolomnya banyak (rentang milestone panjang), grid tetap bisa melebihi 100% dan men-trigger scroll horizontal dari `overflow-x-auto` di div pembungkusnya — ini perilaku default grid+overflow yang benar, tidak perlu trik `width` manual sama sekali.
- **Root cause warna**: varian `akan_datang` sebelumnya pakai `bg-secondary`, dan `lewat` pakai `bg-muted` — di `globals.css`, `--secondary` dan `--muted` kebetulan punya nilai oklch yang **sama persis** (`0.97 0 0`), jadi kedua status itu nyaris tidak terbedakan secara visual. Diganti `akan_datang` jadi hollow/outline (`border-2 border-muted-foreground bg-transparent`, tidak ada isian warna) supaya beda bentuk dari `lewat` yang solid abu-abu muda (`bg-muted`) dan `berlangsung` yang solid gelap (`bg-primary`) — 3 gaya visual yang jelas beda (hollow / solid muda / solid gelap) tanpa menambah warna baru (masih token netral yang sudah ada). Keputusan sengaja begini karena baunya lebih ke bug legibility (dua status beda tidak bisa dibedakan sama sekali) daripada preferensi estetika — kalau cuma soal pemilihan warna yang lebih "fun"/berwarna (bukan soal legibility), itu baru pantas ditunda ke Fase 8 sesuai scope resminya.

### Fase 4 — Bucket Tugas & Breakdown

**Tidak ada migration SQL baru di fase ini.** Tabel `tasks`/`subtasks` dan policy RLS-nya (`tasks_scoped`, `subtasks_scoped` — resolve `kepanitiaan_site_id` lewat join ke `buckets`, dan untuk `subtasks` lewat join ganda `tasks` → `buckets`) sudah dibuat dari migration Fase 1 tapi belum pernah dipakai UI-nya sampai fase ini, jadi murni kerja UI sama seperti Fase 3. Kolom `buckets.is_default` juga sudah ada dari Fase 1 — cuma belum ada cara insert bucket baru dari UI sebelum fase ini.

**Bucket custom baru**: `lib/buckets/actions.ts` — `addBucket(kepanitiaanSiteId, redirectTo, formData)` insert bucket biasa (`is_default: false, is_budgeting: false`). Tidak ada pengecekan role manual di action ini — policy `buckets_scoped` yang sudah ada dari Fase 1 otomatis membatasi insert ke instance milik pemanggil (atau bebas semua instance kalau leader), sama seperti pola RPC kepanitiaan di Fase 2 yang juga mengandalkan RLS langsung tanpa cek role di kode aplikasi.

**Progress bar "sederhana"**: dihitung flat, bukan berjenjang — satu tugas tanpa subtugas dan satu subtugas masing-masing dihitung sebagai 1 item senilai sama, tidak ada bobot berbeda antara tugas induk vs subtugas turunannya (misal tugas dengan 5 subtugas selesai semua tapi status tugasnya sendiri masih "belum" tetap dihitung 5/6, bukan otomatis 100%). Helper di `lib/tasks/progress.ts`:
- `computeProgress(statuses: ItemStatus[])` — dipakai di halaman detail 1 bucket, terima array gabungan status semua task+subtask di bucket itu.
- `computeBucketProgressMap(tasks, subtasks)` — dipakai di halaman daftar bucket (`BucketListSection`), supaya progress semua bucket sekaligus bisa dihitung dari 2 query datar (`tasks` dengan `bucket_id in (...)`, lalu `subtasks` dengan `task_id in (...)`) tanpa query per-bucket yang N+1.

**Struktur komponen — semua Server Component, nol client-side JS** (beda dari Timeline Fase 3 yang butuh `"use client"` untuk fitur edit-lock milestone): expand/collapse tugas dibuat pakai elemen native `<details>`/`<summary>` (browser handle sendiri, tidak perlu `useState`), dan form edit tugas/subtugas selalu terbuka begitu `<details>` di-expand — tidak ada mode "lock" seperti milestone, mengikuti pola `CommitteeMembersSection` (form-per-baris langsung editable) bukan pola `MilestoneRow` (toggle edit-mode).
- `components/buckets/bucket-list-section.tsx` (`BucketListSection`) — daftar bucket, tiap baris jadi `<Link>` ke halaman detail bucket + `ProgressBar` + teks "X/Y tugas & subtugas selesai", plus form "+ Tambah Bidang" di bawah. Dipakai di 2 tempat: `/leader/kepanitiaan/[instanceId]` (basePath `.../bucket`) dan `/panitia/bucket` (basePath `/panitia/bucket`).
- `components/tasks/bucket-tasks-section.tsx` (`BucketTasksSection`) — isi halaman detail 1 bucket: progress bar keseluruhan bucket (union semua task+subtask di bucket itu) + daftar `TaskItem` + form "+ Tambah Tugas" (cuma field judul & deadline opsional — status baru selalu default `belum` lewat DB, tidak ditawarkan di form tambah, sama seperti pola `TimelineSection` yang tidak menawarkan pilihan status milestone).
- `components/tasks/task-item.tsx` (`TaskItem`) — satu `<details>` per tugas. `<summary>` nampilin judul + `Badge` status + deadline + ringkasan "X/Y subtugas selesai" (kalau ada) tanpa perlu expand dulu. Isi expand: form edit tugas (judul/deadline/status, form & tombol hapus dipisah jadi 2 `<form>` sibling — bukan nested, karena HTML tidak boleh nested `<form>`) + daftar `SubtaskRow` + form "+ Tambah Subtugas".
- `components/tasks/subtask-row.tsx` (`SubtaskRow`) — form-per-baris subtugas (judul/deadline/status langsung editable + tombol hapus terpisah), pola identik baris `CommitteeMembersSection`.
- `components/buckets/progress-bar.tsx` (`ProgressBar`) — komponen dumb, `<div>` isi dengan `width: {percent}%` pakai token warna yang sudah ada (`bg-muted` track, `bg-primary` isi) — belum ada warna baru, konsisten dengan keputusan Fase 3 soal warna "fun" ditunda ke Fase 8.

**Actions**: `lib/tasks/actions.ts` (`addTask`/`updateTask`/`deleteTask`/`addSubtask`/`updateSubtask`/`deleteSubtask`, tipe `ItemStatus = "belum" | "proses" | "selesai"` di-export dari sini) dan `lib/buckets/actions.ts` (`addBucket`) — pola identik `lib/committee/actions.ts`/`lib/timeline/actions.ts` (`redirect` ke `?error=...` kalau gagal, `revalidatePath` kalau sukses). `parseStatus()` fallback ke `"belum"` kalau value yang dikirim bukan salah satu dari 3 pilihan yang valid (defense-in-depth terhadap form yang dimanipulasi manual lewat devtools).

**Refactor kecil**: constant CSS untuk native `<select>` yang sebelumnya cuma lokal (`SELECT_CLASSNAME` di `committee-members-section.tsx`) dipindah jadi export bersama `NATIVE_SELECT_CLASSNAME` di `lib/utils.ts`, supaya `TaskItem` dan `SubtaskRow` bisa pakai styling yang sama tanpa duplikasi 3x. `committee-members-section.tsx` diupdate untuk import versi shared ini — tidak ada perubahan visual atau behavior di section itu.

**Halaman baru**:
- Leader: `/leader/kepanitiaan/[instanceId]/bucket/[bucketId]` — fetch bucket dengan filter ganda `.eq("id", bucketId).eq("kepanitiaan_site_id", instanceId)` (defense-in-depth walau RLS `buckets_scoped` sudah cukup — kalau tidak match, `notFound()`), lalu tasks + subtasks di bucket itu, render `BucketTasksSection`. Link "← Kembali ke detail instance".
- Panitia: `/panitia/bucket` (daftar bucket instance sendiri, resolve `kepanitiaan_site_id` dari `public.users` di server — pola identik `/panitia/susunan`/`/panitia/timeline`) dan `/panitia/bucket/[bucketId]` (detail 1 bucket, query bucket-nya juga di-filter `.eq("kepanitiaan_site_id", kepanitiaanSiteId)` supaya panitia tidak bisa buka bucket instance lain lewat tebak UUID di address bar — kalau lolos filter ini pun RLS `buckets_scoped`/`tasks_scoped`/`subtasks_scoped` jadi lapis kedua).

**Halaman yang diubah**: `/leader/kepanitiaan/[instanceId]` — blok badge bucket read-only (cuma nampilin nama, tidak bisa diklik) diganti jadi `BucketListSection` (sekarang bisa diklik ke halaman detail, ada progress bar, ada form "+ Tambah Bidang"). `/panitia/dashboard` — tombol "Bucket Tugas" ditambah (link ke `/panitia/bucket`), teks placeholder "Bucket tugas akan diimplementasikan mulai Fase 4" dihapus.

**Keputusan: bucket "Budgeting" (`is_budgeting=true`) diperlakukan sama seperti bucket biasa di fase ini** — render `BucketTasksSection` yang sama, bisa diisi tugas/subtugas biasa (misal "Siapkan draft budget", "Kumpulkan nota"). HANDOVER section 5 bilang bucket ini seharusnya render "widget khusus download-template + upload-submission" begitu Fase 5 selesai — itu belum dibangun di sini, sengaja ditunda karena di luar scope resmi Fase 4 ("Halaman per bucket: daftar tugas besar → expand ke subtugas"). Konsekuensinya: sesi Fase 5 perlu memutuskan apakah widget budgeting itu ditaruh **menggantikan** daftar tugas di halaman ini atau **berdampingan** dengannya (leader mungkin masih mau catat tugas persiapan budget di sini) — belum diputuskan, dicatat di sini supaya tidak diputuskan diam-diam tanpa disadari.

**Tidak ada delete/rename bucket** di fase ini — section 6 Fase 4 di HANDOVER cuma minta "Tombol + Tambah Bidang", tidak minta hapus/ubah nama bucket (beda dari kepanitiaan/instance di Fase 2 yang eksplisit diminta Yolanda saat verifikasi live sesi itu). Kalau nanti dibutuhkan, ikuti pola `renameKepanitiaan`/`deleteKepanitiaan` di `lib/kepanitiaan/actions.ts` (termasuk pengecekan "masih dipakai atau tidak" sebelum hapus, kalau relevan untuk bucket yang masih ada tugasnya).

**Verifikasi yang sudah dilakukan (di sandbox, bukan live)**: `npm install`, `next build`, `next lint`, `tsc --noEmit` — semua sukses dengan `.env.local` placeholder sementara (dihapus lagi sebelum commit, tidak pernah masuk git, pola sama seperti Fase 2/3). Ketiga route baru (`/leader/kepanitiaan/[instanceId]/bucket/[bucketId]`, `/panitia/bucket`, `/panitia/bucket/[bucketId]`) muncul di output `next build` sebagai route dinamis (`ƒ`). Koneksi nyata ke Supabase tetap tidak bisa dites dari sandbox ini (kendala yang sama seperti semua fase sebelumnya).

**Verifikasi live yang masih perlu dilakukan Yolanda** (belum sempat dicek sama sekali dari sandbox — checklist ini yang jadi syarat centang Fase 4 di section 8):
1. Buka instance yang sudah ada lewat `/leader/kepanitiaan/[instanceId]` — pastikan tiap bucket sekarang jadi baris yang bisa diklik dengan progress bar (harusnya 0% karena belum ada tugas).
2. Klik salah satu bucket — pastikan masuk ke halaman detail bucket, coba "+ Tambah Tugas" (isi nama + deadline opsional) — tugas baru harus muncul sebagai `<details>` tertutup dengan badge status "Belum".
3. Klik tugas untuk expand — ubah status ke "Proses"/"Selesai", "Simpan" — pastikan badge di `<summary>` ikut berubah setelah collapse lagi. Coba "+ Tambah Subtugas", ubah status subtugas — pastikan progress bar (baik di halaman detail bucket maupun di daftar bucket) ikut naik sesuai jumlah item berstatus "selesai".
4. Coba "Hapus Tugas" pada tugas yang sudah punya subtugas — pastikan subtugasnya ikut hilang (cascade dari FK `subtasks.task_id`), dan progress bar ikut turun sesuai jumlah item yang hilang.
5. Di halaman daftar bucket (`/leader/kepanitiaan/[instanceId]` maupun `/panitia/bucket`), coba "+ Tambah Bidang" bikin bucket custom baru — pastikan muncul di daftar dengan progress 0%, dan bisa diisi tugas seperti bucket default.
6. Login sebagai `panitia-a@test.local` (instance rujukannya ada di `supabase/verify_fase3.sql`), buka `/panitia/bucket` — pastikan cuma bucket instance-nya sendiri yang muncul, tambah 1 tugas percobaan di sana. Logout, login sebagai `panitia-b@test.local` — pastikan tugas yang baru ditambahkan panitia-a **tidak** muncul di instance panitia-b. Ini menutup isolasi RLS `tasks_scoped`/`subtasks_scoped` yang sudah ada sejak Fase 1 tapi belum pernah benar-benar dites lewat UI sampai sekarang.
7. Sambil masih login sebagai panitia (a atau b), coba akses langsung URL `/panitia/bucket/<uuid-bucket-milik-instance-lain>` (copy UUID bucket dari instance lain lewat akun leader) — harus kena halaman 404 (`notFound()`), bukan malah menampilkan data bucket instance lain.

**Revisi setelah review Yolanda (ronde 1)** — 9 permintaan, dikerjakan di sesi yang sama sebelum verifikasi live sempat selesai, jadi checklist 7 poin di atas masih berlaku dan sekarang meliputi UI hasil revisi juga:

1. **"Leader kehilangan akses ke timeline & tidak ada bucket tugas" — ternyata bukan bug, tapi navigasi yang tidak jelas.** Timeline/Bucket/Susunan Panitia memang hidup **di dalam tiap instance** (`/leader/kepanitiaan/[instanceId]`), bukan sebagai menu global di dashboard leader — leader harus drill-in ke site dulu. Yang salah cuma petunjuknya: tombol dashboard leader dulu berlabel "Kelola Kepanitiaan & Susunan Panitia" (tidak menyebut Timeline/Bucket sama sekali). Diperbaiki dengan menambah teks penjelas di `/leader/dashboard` dan `/leader/kepanitiaan` ("pilih site untuk membuka Timeline, Bucket Tugas, dan Susunan Panitia"). **Tidak ada perubahan struktur route atau RLS** — kalau di kemudian hari leader mau punya akses lintas-instance dalam satu layar, itu memang scope Fase 6 (Dashboard Kepanitiaan), bukan perbaikan navigasi ini.
2. **Navigasi Home & Kembali** — komponen baru `components/page-nav.tsx` (`PageNav`, `"use client"` karena pakai `useRouter().back()`), dipasang di semua halaman dalam: bucket (list & detail), timeline, susunan panitia, manajemen kepanitiaan, dan detail instance. "Kembali" pakai history browser (jadi benar-benar halaman sebelumnya, bukan parent route yang ditebak), "Home" ke dashboard sesuai role lewat prop `homeHref` (komponen client tidak bisa query role sendiri). Link ad-hoc "← Kembali ke ..." yang sebelumnya cuma ada di sebagian halaman dihapus, diganti komponen ini.
3. **Warna Gantt "berlangsung" tidak akurat** — sebelumnya `bg-primary` (hitam polos), tidak kebaca sebagai "sedang jalan". Sekarang tiap status punya hue sendiri di `timeline-gantt.tsx`: biru + ring (berlangsung, paling menonjol), abu solid (lewat), hollow biru muda (akan datang). Ini melanggar sedikit prinsip "warna ditunda ke Fase 8" yang dipegang di Fase 3, tapi alasannya sama seperti bugfix legibility Gantt sebelumnya: statusnya tidak terbaca, bukan sekadar kurang cantik.
4. **Susunan panitia dikelompokkan per bidang, leader bidang di posisi teratas** — tiap bidang jadi grup dengan heading + jumlah orang; di dalam grup, `role = leader_bidang` selalu diurutkan paling atas (`sortLeaderFirst`), sisanya menjaga urutan input. Baris anggota diekstrak ke `components/committee/committee-member-row.tsx`.
   - **Konsekuensi yang disengaja — membalik keputusan Fase 3**: `parseBucketId()` di `lib/committee/actions.ts` dulu selalu me-null-kan `bucket_id` kalau role bukan `leader_bidang`. Kalau itu dipertahankan, semua anggota biasa jatuh ke grup "Tanpa Bidang" dan pengelompokan ini jadi percuma — jadi sekarang `bucket_id` berlaku untuk **semua role**, dan role cuma menentukan urutan di dalam grup. Catatan Fase 3 poin 4 yang bilang "bucket_id di-null-kan untuk anggota biasa" **sudah tidak berlaku lagi**.
   - Anggota lama yang `bucket_id`-nya terlanjur null tetap ditampilkan di grup "Tanpa Bidang" (dengan ajakan memilih bidang lalu Simpan) supaya tidak hilang diam-diam dari halaman.
5. **Status tugas jadi single-choice yang kelihatan sekaligus** — `components/tasks/status-choice.tsx` (`StatusChoice`): radio HTML biasa yang di-style jadi chip berwarna lewat `peer`/`peer-checked`, jadi ketiga pilihan kebaca tanpa buka dropdown dan tetap nol client-side JS. Label diganti jadi **Assigned / In Progress / Done** sesuai istilah yang diminta — **nilai di DB tetap `belum`/`proses`/`selesai`**, jadi check constraint dari Fase 1 tidak perlu diubah dan tidak ada migration untuk ini. Pemetaan labelnya ada di `STATUS_LABEL` (`lib/tasks/format.ts`) — kalau nanti mau balik ke istilah Indonesia, cukup ubah di situ.
6. **Assign tugas/subtugas ke anggota panitia** — migration baru `20260810000006_fase4_task_assignment.sql`: kolom `assignee_id` di `tasks` dan `subtasks` (FK ke `committee_members`, `on delete set null` — menghapus anggota tidak ikut menghapus tugasnya, cuma jadi belum-ada-penanggung-jawab), plus trigger `trg_check_task_assignee_instance`/`trg_check_subtask_assignee_instance` yang memvalidasi assignee harus anggota di instance yang sama (pola persis sama seperti trigger `bucket_id` di migration Fase 3 — UI cuma menawarkan anggota yang benar, tapi tidak ada yang mencegah request manual ke REST API). Tidak ada perubahan RLS: policy `tasks_scoped`/`subtasks_scoped` berlaku per-baris, jadi kolom baru ikut terlindungi otomatis. UI-nya `components/tasks/assignee-select.tsx` (dropdown dengan opsi "— Belum di-assign —"), dan nama assignee muncul sebagai badge di ringkasan tugas tanpa perlu expand.
7. **Board bucket gaya Trello** — `components/buckets/bucket-board.tsx` (`BucketBoard`): tiap bidang jadi kolom vertikal selebar `w-72` di dalam `overflow-x-auto`, tugas jadi kartu yang menumpuk ke bawah (judul, chip status berwarna, hitungan subtugas, deadline, nama assignee). **Board ini read-only** — CRUD tugas tetap di halaman detail bucket, jadi tidak perlu drag-and-drop maupun client-side JS. Menggantikan daftar bucket bergaya list yang dipakai versi pertama Fase 4.
8. **Master progres per instance** — `components/buckets/master-progress.tsx` (`MasterProgress`): persentase gabungan seluruh tugas & subtugas dari **semua** bucket di satu instance, plus hitungan berapa bidang yang sudah 100%. Bucket yang belum punya tugas sama sekali sengaja **tidak** dihitung selesai (0/0 = belum dikerjakan, bukan beres). Ditampilkan di atas board, baik di halaman leader maupun panitia.
9. **`lib/buckets/board-data.ts` (`loadBucketBoardData`)** — satu loader dipakai bareng halaman leader (`/leader/kepanitiaan/[instanceId]`) dan panitia (`/panitia/bucket`), supaya keduanya tidak menghitung progres dengan cara yang berbeda kalau salah satunya diubah nanti. Query-nya tetap datar (`tasks` dengan `bucket_id in (...)`, `subtasks` dengan `task_id in (...)`, `committee_members` sekali), bukan per-bucket, jadi tidak N+1.

**Langkah verifikasi live tambahan untuk revisi ronde 1** (di luar 7 poin sebelumnya):
8. Jalankan migration `20260810000006_fase4_task_assignment.sql` lewat Supabase Dashboard > SQL Editor (setelah `...0001` s/d `...0005`). Ini satu-satunya migration baru dari ronde revisi ini.
9. Buka halaman bucket (leader maupun panitia) — pastikan tampil sebagai board kolom-per-bidang yang bisa di-scroll horizontal, dengan card Master Progres di atasnya. Cek persentase master naik setelah menandai satu tugas jadi "Done".
10. Di detail bucket, ubah status tugas lewat chip Assigned/In Progress/Done — pastikan chip yang aktif jelas kelihatan berbeda, dan statusnya tersimpan setelah refresh.
11. Assign satu tugas ke anggota panitia — pastikan namanya muncul sebagai badge di ringkasan tugas **dan** di kartu tugas pada board. Cek juga dropdown assignee cuma berisi anggota instance itu sendiri.
12. Di susunan panitia, pastikan anggota terkelompok per bidang dengan Leader Bidang di posisi teratas tiap grup. Anggota lama yang belum punya bidang harus muncul di grup "Tanpa Bidang" — pilih bidangnya lalu Simpan, pastikan pindah ke grup yang benar.
13. Cek tombol "Kembali" dan "Home" muncul & berfungsi di halaman bucket, timeline, susunan panitia, manajemen kepanitiaan, dan detail instance.

**Revisi setelah review Yolanda (ronde 2)** — dikerjakan setelah verifikasi live pertama berhasil dijalankan di laptop Yolanda (board Trello & assign anggota sudah dikonfirmasi jalan). Poin verifikasi 1-13 di atas tetap berlaku, ditambah poin 14-19 di bawah:

1. **Bugfix: tombol Simpan status tugas tidak bisa diklik.** Root cause: `StatusChoice` membungkus chip statusnya dengan `<fieldset>`/`<legend>` di dalam baris `flex`. Fieldset punya aturan layout bawaan (`min-inline-size: min-content`, dan `<legend>` dirender di luar flow normal) sehingga elemen itu tidak mau menyusut mengikuti baris flex — akibatnya elemen sesudahnya (tombol Simpan) terdorong keluar area yang bisa diklik. Diganti `<div>` biasa + `<span>` sebagai label. Radio-nya juga tidak lagi pakai `sr-only` (yang `position: absolute` tanpa jangkar `relative`), melainkan `absolute size-0 opacity-0` di dalam pembungkus `relative`. **Pelajaran umum: jangan pakai `<fieldset>` sebagai anak langsung baris flex di codebase ini.**

2. **Bugfix: tombol Simpan "tidak terlihat responsif".** Dua penyebab terpisah, keduanya diperbaiki lewat komponen baru `components/submit-button.tsx` (`SubmitButton`):
   - *Kontras*: tombol Simpan sebelumnya `variant="secondary"`, yaitu `bg-secondary` = `oklch(0.97 0 0)` — abu-abu 97% lightness di atas card putih, praktis tidak terlihat. (Ini token yang sama yang sudah pernah bikin masalah legibility di bar Gantt Fase 3 — kalau butuh tombol sekunder yang tetap kebaca, jangan pakai `secondary` polos.)
   - *Umpan balik*: tidak ada indikator pending sama sekali; server action jalan diam-diam lalu halaman revalidate. `SubmitButton` pakai `useFormStatus()` untuk menonaktifkan tombol + menampilkan spinner + label "Menyimpan…". Catatan implementasi: `useFormStatus()` **harus** dipanggil dari komponen anak `<form>`, bukan dari komponen yang merender `<form>`-nya — makanya ini komponen terpisah, bukan prop di `Button`.

3. **Upload logo/foto kepanitiaan (Supabase Storage)** — migration baru `20260810000007_fase4_logo_kepanitiaan_storage.sql`. **Ini migration pertama di project yang menyentuh Storage.**
   - Kolom `kepanitiaan.logo_url` (text, nullable) menyimpan URL publik hasil upload, bukan file-nya.
   - Bucket `kepanitiaan-logo` dibuat lewat SQL (bukan klik-klik Dashboard) supaya langkahnya ikut tercatat di repo dan bisa diulang di project Supabase baru. `public = true` karena logo bukan data sensitif dan URL publik bisa dipakai langsung di `<img src>` tanpa signed URL yang kedaluwarsa — yang dibatasi adalah siapa yang boleh mengunggah, bukan siapa yang boleh melihat. `file_size_limit` 2 MB dan `allowed_mime_types` PNG/JPEG dipasang **di level bucket**, jadi file tidak sesuai ditolak Storage sendiri, bukan cuma oleh validasi server action.
   - Policy `storage.objects`: baca bebas, insert/update/delete hanya leader lewat helper `public.is_leader()` yang sama dengan RLS tabel lain (logo menempel ke `kepanitiaan`, yang memang cuma boleh dikelola leader).
   - `lib/kepanitiaan/logo-actions.ts`: nama objek di-generate dari **id kepanitiaan**, bukan nama file asli (nama asli bisa mengandung karakter aneh dan bisa dipakai menimpa objek lain). Pakai `upsert: true` supaya satu kepanitiaan = satu objek dan tidak menumpuk file yatim. URL disimpan dengan query `?v=<timestamp>` — tanpa ini, path yang sama akan tetap menampilkan logo lama dari cache browser/CDN setelah diganti. `removeKepanitiaanLogo` menghapus `.png` **dan** `.jpg` sekaligus, karena logo yang pernah berganti format menyisakan objek lama.
   - Dipakai lewat `<img>` polos, bukan `next/image` — URL-nya dinamis per kepanitiaan dan `next/image` butuh `remotePatterns` yang di-hardcode ke host project Supabase.

4. **Section di dalam instance jadi tab menyamping, bukan accordion ke bawah** — `components/ui/side-tabs.tsx` (`SideTabs`, `"use client"`). Konten tiap tab dirender di **server** lalu dioper sebagai prop `content: ReactNode`, jadi komponen ini boleh client tanpa memaksa section di dalamnya ikut jadi client component. Semua tab dirender sekaligus dan yang tidak aktif disembunyikan lewat atribut `hidden`, **bukan di-unmount** — kalau di-unmount, isian form yang sedang diketik di tab lain ikut hilang saat berpindah tab. Komponen `AccordionSection` (dari ronde 1) masih dipakai di halaman panitia yang cuma punya satu section.

5. **Manajemen Kepanitiaan jadi terkunci + hierarki master event diperjelas** — `components/kepanitiaan/kepanitiaan-header.tsx` (`KepanitiaanHeader`, client): "Simpan Nama", "Hapus Kepanitiaan", dan form upload logo baru muncul setelah klik **Edit**. Nama master event (PON/FIND/Habiha) dibesarkan jadi `text-2xl font-bold` + keterangan "Master event · N site" supaya hubungan master event → instance kebaca. `LogoPlaceholder` dari ronde 1 dihapus, digantikan komponen ini (yang menampilkan `<img>` kalau `logo_url` ada, atau kotak putus-putus kalau belum).

6. **Pemisah antar-bidang di Susunan Panitia** — tiap bidang sekarang dibungkus kartu sendiri: `rounded-lg border-2` + garis aksen kiri (`border-l-4 border-l-primary`) + header abu (`bg-muted/60`) berisi nama bidang dan jumlah orang. Grup "Tanpa Bidang" dapat perlakuan sama tapi aksen kirinya `border-l-muted-foreground` supaya tidak terlihat setara dengan bidang sungguhan.

**Langkah verifikasi live tambahan untuk revisi ronde 2:**
14. Jalankan migration `20260810000007_fase4_logo_kepanitiaan_storage.sql` lewat SQL Editor. Verifikasi bucket-nya jadi: Dashboard > Storage harus memunculkan bucket **kepanitiaan-logo**.
15. Di `/leader/kepanitiaan`, pastikan tombol Simpan Nama/Hapus Kepanitiaan **tidak** muncul sampai klik Edit, dan nama master event tampil besar dengan keterangan jumlah site.
16. Klik Edit > pilih file PNG/JPG > Upload Logo — logo harus muncul menggantikan kotak putus-putus. Coba juga ganti logo dengan file lain, pastikan yang tampil langsung yang baru (bukan versi lama dari cache). Coba upload file >2 MB atau format selain PNG/JPG — harus ditolak dengan pesan yang jelas, bukan error mentah.
17. Coba upload logo dari **akun panitia** (kalau bisa mengakses halamannya) — harus gagal karena policy `kepanitiaan_logo_insert_leader`.
18. Buka satu instance — pastikan tiga section tampil sebagai tab menyamping. Ketik sesuatu di form tambah anggota, pindah tab, lalu balik — isian tidak boleh hilang.
19. Klik Edit di satu anggota panitia, klik Simpan — tombolnya harus jelas terlihat (gelap, bukan putih pudar) dan menampilkan spinner + "Menyimpan…" selama proses.

**Revisi setelah review Yolanda (ronde 3)** — dikerjakan setelah verifikasi live ronde 2 (upload logo & tab menyamping sudah dikonfirmasi jalan). Poin 1-19 di atas tetap berlaku, ditambah poin 20-26:

1. **Bugfix "Simpan tidak terlihat reaktif" (lanjutan)** — `SubmitButton` (dari ronde 2) sudah punya spinner+disabled selama pending, tapi begitu request selesai (biasanya cuma beberapa ratus ms), form langsung tertutup dan baris berubah ke tampilan terkunci tanpa jeda apapun -- perubahan terlalu instan untuk terasa sebagai "reaksi terhadap klik barusan". Ditambah `lib/hooks/use-save-flash.ts` (`useSaveFlash`): setelah `handleSave` selesai, baris (task/subtask) dapat highlight hijau + teks "✓ Tersimpan" selama ~1.6 detik sebelum balik normal. Dipasang di `TaskItem` dan `SubtaskRow`. `SubmitButton` juga ditambah prop `pending` eksplisit (di-OR dengan `useFormStatus()`) untuk kasus form yang statusnya lebih tepat dikontrol dari pemanggil.

2. **Susunan Panitia: root cause "per bidang cuma bisa 1 orang" ditelusuri habis, tidak ditemukan limit apapun di kode/skema** — sudah dicek: tidak ada `unique` constraint di `committee_members` (migration Fase 1 & Fase 3 di-grep ulang), `addCommitteeMember` di `lib/committee/actions.ts` pakai `insert()` polos (bukan upsert), dan logika grouping di `CommitteeMembersSection` (`buckets.map` + `members.filter(m => m.bucket_id === bucket.id)`) tidak mengandung `.slice`/dedup/limit apapun -- disimulasikan manual dengan 2 anggota `bucket_id` sama, hasilnya keduanya di-map dan di-render. Kesimpulan: **render/DB mendukung N anggota per bidang tanpa batas**. Dugaan paling mungkin: satu-satunya form tambah anggota sebelumnya ada di **paling bawah seluruh halaman**, jauh dari kartu bidang, dengan dropdown "Bidang Tugas" yang `defaultValue`-nya selalu balik ke bucket pertama tiap render -- kombinasi ini gampang membuat leader tidak sadar sedang menambah ke bidang yang salah, atau malah mengklik Edit pada anggota yang sudah ada (mengira itu "tambah baru").
   - **Perbaikan**: form tambah anggota dipindah dari satu form global jadi tombol "+ Tambah Anggota" **di dalam tiap kartu bidang** (`components/committee/add-member-row.tsx`, `AddMemberRow`) -- `bucket_id` dikirim lewat `<input type="hidden">` yang sudah pasti benar dari kartu mana tombolnya diklik, bukan dropdown pilihan. Ini juga sekaligus jadi jawaban untuk keluhan "bidang kosong tidak ada cara ditambahi" -- sekarang tombolnya ada tepat di dalam kartu yang kosong itu.
   - Grup "Tanpa Bidang" **tidak** dapat tombol tambah baru -- bidang sudah wajib untuk semua role sejak ronde 1, jadi tidak masuk akal menambah anggota baru langsung ke situ. Anggota yang muncul di grup ini cuma sisa data lama.

3. **Warna kapsul status jadi pink/kuning/hijau** (sebelumnya slate/amber/emerald) -- diekstrak ke komponen bersama `components/tasks/status-pill.tsx` (`StatusPill`) supaya satu sumber kebenaran warna, dipakai di `TaskItem`, `SubtaskRow`, `BucketBoard`, dan (lewat `STATUS_CHIP_CLASSNAME` yang di-reuse) `StatusChoice`. Assigned = pink, In Progress = kuning, Done = hijau.

4. **Tab baru "Tugas Saya" di `/panitia/workspace`**, urutan tab sekarang: Progres & Task Board → Tugas Saya → Timeline Pelaksanaan → Susunan Panitia.
   - **Keputusan kunci: pencocokan akun ke anggota panitia lewat EMAIL, bukan kolom relasi baru.** Skema `committee_members` tidak punya `user_id` yang menautkan ke `auth.users`/`public.users` -- menambah kolom itu berarti migration baru + alur pengisian yang harus dipikirkan (siapa yang isi, kapan). Karena kolom `email` sudah ada di kedua sisi (akun Supabase Auth punya email login, `committee_members.email` sudah ada sejak Fase 1), dipakai sebagai kunci pencocokan (`ilike`, case-insensitive) tanpa migration baru. **Konsekuensi penting**: fitur ini cuma jalan kalau leader mengisi `committee_members.email` dengan email **login** panitia yang sama persis -- ini bukan validasi ketat di level DB, jadi salah ketik email membuat "Tugas Saya" kosong tanpa error yang jelas selain pesan generik di poin berikut.
   - `lib/tasks/my-tasks.ts` (`loadMyTasks`): query `committee_members` by email match dulu untuk dapat `matchedMemberIds`, baru query `tasks`/`subtasks` dengan `assignee_id in (...)`, gabung jadi satu list terurut deadline (null di akhir). Kalau tidak ada match, `components/tasks/my-tasks-section.tsx` (`MyTasksSection`) menampilkan pesan eksplisit "Hubungi leader untuk menambahkan Anda ke Susunan Panitia dengan email yang sama" -- bukan halaman kosong tanpa penjelasan.
   - Read-only, tiap baris link ke halaman detail bucket tempat tugas itu berada untuk di-edit (tugas di "Tugas Saya" bisa berasal dari bidang manapun, jadi tidak ada satu form edit yang masuk akal ditaruh langsung di halaman ini).

5. **Rename aplikasi jadi "PanitYAY"** -- diterapkan di `<title>` (`app/layout.tsx`), judul halaman login (`app/(auth)/login/page.tsx`), dan pesan error login yang sebelumnya menyebut "sistem orientasi panitia" (`lib/auth/actions.ts`). **Sengaja tidak menyentuh**: nama folder/route (semua path `/leader/...`, `/panitia/...` tetap sama), `package.json` `name`, dan `README.md` -- permintaannya spesifik "penamaan di dalam page dan di URL nantinya", jadi struktur URL/folder dipertahankan; URL custom domain (kalau nanti di-deploy dengan domain sendiri) di luar scope kode ini.

**Tidak ada migration baru di ronde 3 ini** -- semua perubahan murni UI + query baca (email match untuk Tugas Saya, tidak ada kolom baru).

**Verifikasi yang sudah dilakukan (di sandbox, bukan live)**: `npm run build`, `npm run lint`, `tsc --noEmit` -- semua bersih. Smoke test dev server: `/login` merender teks "PanitYAY".

**Verifikasi live tambahan untuk ronde 3:**
20. Klik Simpan di edit tugas/subtugas -- pastikan setelah form tertutup, baris itu sempat menyala hijau + teks "Tersimpan" sesaat sebelum balik normal.
21. Buka Susunan Panitia, klik "+ Tambah Anggota" di dalam SATU kartu bidang yang sudah punya anggota -- tambahkan anggota ke-2, pastikan MUNCUL DUA orang di bidang itu (bukan menimpa yang pertama). Ulangi di bidang lain untuk pastikan hidden `bucket_id`-nya benar per kartu.
22. Cek kapsul status: Assigned = pink, In Progress = kuning, Done = hijau, konsisten di board maupun ringkasan tugas.
23. Login sebagai panitia yang `committee_members.email`-nya SUDAH diisi sama dengan email login -- buka `/panitia/workspace` tab "Tugas Saya", pastikan tugas yang di-assign ke dia muncul, terurut deadline.
24. Login sebagai panitia yang emailnya BELUM cocok dengan anggota manapun -- pastikan tab "Tugas Saya" menampilkan pesan "hubungi leader", bukan kosong tanpa keterangan.
25. Klik salah satu baris di "Tugas Saya" -- pastikan masuk ke halaman detail bucket yang benar (bukan bucket lain).
26. Cek `<title>` tab browser dan halaman login menampilkan "PanitYAY", dan coba login gagal (password salah/akun belum terdaftar) -- pesan errornya juga menyebut "PanitYAY", bukan nama lama.

**Revisi setelah review Yolanda (ronde 4)** — koreksi typo nama + satu perubahan perilaku yang cukup penting (batasi tambah anggota ke akun terdaftar). Poin 1-26 di atas tetap berlaku, ditambah poin 27-29:

27. **Koreksi nama: "PanitYAY" -> "PanitiYAY"** -- typo di ronde 3. Diperbaiki di `<title>` (`app/layout.tsx`), halaman login, dan pesan error login (`lib/auth/actions.ts`). Welcome page panitia (`/panitia/dashboard`) diubah jadi persis "Selamat datang di PanitiYAY!" sesuai permintaan.

28. **"+ Tambah Anggota" sekarang cuma bisa pilih dari akun panitia yang sudah terdaftar, bukan ketik email bebas** -- permintaan eksplisit Yolanda: "agar kita bisa menjaga sinkronisasi datanya sedari dini". Sebelumnya field Email di `AddMemberRow` (ronde 3) adalah `<input type="email">` bebas ketik -- risikonya persis yang bikin fitur "Tugas Saya" (ronde 3, poin 4) rawan gagal diam-diam: typo email di committee_members tidak akan cocok dengan email login manapun, dan tidak ada validasi yang menangkap itu saat disimpan.
    - **Migration baru** `20260810000008_fase4_pilih_akun_terdaftar.sql`: RPC `list_akun_panitia_instance(p_kepanitiaan_site_id uuid)` mengembalikan `(id, email)` akun `public.users` ber-`role='panitia'` untuk satu instance. **Kenapa perlu RPC, bukan query `public.users` langsung**: policy `users_select_own_or_leader` dari Fase 1 cuma mengizinkan akun lihat baris sendiri (atau semua kalau leader) -- akun panitia tidak bisa SELECT baris panitia lain lewat query biasa, termasuk peer di instance yang sama. RPC ini `security definer` (pola sama seperti `is_leader()`/`current_user_role()` di Fase 1) supaya bisa membaca `public.users` lepas dari RLS-nya sendiri, **tapi otorisasinya tetap dijaga manual di WHERE clause**: `is_leader() OR current_user_kepanitiaan_site_id() = p_kepanitiaan_site_id` -- baris selain instance yang berhak tidak akan pernah ikut ke-return, jadi tidak ada kebocoran data lintas-instance meskipun function-nya bypass RLS.
    - `lib/committee/candidate-accounts.ts` (`loadCandidateAccounts`) memanggil RPC ini, dipakai di kedua halaman yang merender `CommitteeMembersSection` (leader & panitia workspace) dan dioper sebagai prop `candidateAccounts`.
    - `AddMemberRow` (`components/committee/add-member-row.tsx`): field Email diganti jadi `<select>` berisi daftar `candidateAccounts` (value = email langsung, tidak perlu mapping id->email terpisah). Field Nama **tetap free text** -- `public.users` tidak punya kolom nama, jadi tidak ada sumber data untuk dijadikan pilihan.
    - **Kalau `candidateAccounts` kosong** (belum ada akun panitia yang di-link ke instance itu di `public.users`), `AddMemberRow` menampilkan pesan eksplisit yang mengarahkan ke Supabase Dashboard > Authentication + isi `kepanitiaan_site_id` di `public.users` -- bukan form kosong yang membingungkan.
    - **Konsekuensi workflow yang perlu disadari**: sejak ronde ini, urutan kerja yang benar jadi **1) buat akun login panitia dulu (manual, Supabase Dashboard, seperti sudah dicatat di Fase 1)** → **2) baru bisa ditambahkan ke Susunan Panitia lewat app**. Sebelumnya urutan bisa dibalik (tambah nama dulu di Susunan Panitia, akun login menyusul, hubungkan manual by-typing email). HANDOVER Fase 1 sudah mencatat pembuatan akun tetap manual lewat Dashboard (tidak ada trigger auto-insert `auth.users` -> `public.users`) -- itu tidak berubah, cuma urutannya sekarang lebih ketat ditegakkan di UI.
    - **Tidak difilter**: akun yang sudah dipakai di committee_members lain di instance yang sama tetap muncul di dropdown (tidak dihapus dari daftar pilihan) -- kalau perlu satu akun tercatat di lebih dari satu baris committee_members (misal bantu 2 bidang), itu masih memungkinkan meskipun modelnya (1 baris = 1 bidang) tidak dirancang untuk kasus itu secara eksplisit.

29. **Field email pada form EDIT anggota (`CommitteeMemberRow`) SENGAJA tidak ikut dibatasi** -- tetap `<input type="email">` bebas seperti sebelumnya. Keputusan cakupan: permintaan "sedari dini" ditafsirkan sebagai titik masuk data baru (saat menambah), bukan mengubah histori entri lama yang emailnya mungkin sudah tidak cocok skema baru ini. Kalau nanti mau diperluas ke edit juga, ikuti pola yang sama (`candidateAccounts` sudah tersedia sebagai prop di `CommitteeMembersSection`, cukum diteruskan ke `CommitteeMemberRow`).

**Verifikasi live tambahan untuk ronde 4:**
27. Jalankan migration `20260810000008_fase4_pilih_akun_terdaftar.sql` lewat SQL Editor (setelah `...0001` s/d `...0007`).
28. Buka `<title>` tab browser dan halaman login -- pastikan "PanitiYAY" (bukan "PanitYAY"). Buka `/panitia/dashboard` -- pastikan tulisan "Selamat datang di PanitiYAY!" muncul.
29. Di Susunan Panitia (leader maupun panitia), klik "+ Tambah Anggota" di kartu bidang manapun -- pastikan field Email sekarang dropdown, bukan kotak ketik. Kalau instance itu SUDAH punya akun panitia terdaftar (misal panitia-a/b yang sudah di-link lewat `verify_fase3.sql`), pastikan email mereka muncul di pilihan.
30. Coba klik "+ Tambah Anggota" di instance yang BELUM punya akun panitia terdaftar sama sekali -- pastikan muncul pesan mengarahkan ke Supabase Dashboard, bukan form kosong atau error.
31. Tambahkan panitia-a (atau panitia-b) lewat dropdown ini ke satu bidang -- pastikan tersimpan dengan email yang benar (cek lewat klik Edit di baris barunya), lalu login sebagai akun itu dan cek tab "Tugas Saya" bisa cocok (karena email-nya sekarang pasti sama, tidak ada typo).

**Ronde 5 (setelah ronde 4 verified live oleh Yolanda) -- catatan state akun test + skrip verifikasi baru:**

- **panitia-a@test.local dan panitia-b@test.local sekarang di INSTANCE YANG SAMA** (Habiha @ Jakarta) -- dipindah dari setup Fase 3 (yang sengaja menaruh mereka di 2 instance berbeda untuk uji isolasi RLS) supaya bisa dites skenario "dua orang bisa di-assign tugas di satu instance yang sama" (bukti fix bug "per bidang cuma 1 orang" dari ronde 4). Dikonfirmasi live: setelah dipindah, keduanya muncul bersamaan di dropdown "+ Tambah Anggota"/"Assign ke" untuk instance Habiha@Jakarta.
- **Konsekuensi**: setup isolasi RLS lama (panitia-a vs panitia-b di 2 instance) sudah tidak berlaku. Isolasi RLS `tasks_scoped`/`subtasks_scoped` dari sisi akun panitia **masih belum pernah dites lewat UI sejak dibuat di Fase 1** -- gap ini terbawa dari checklist ronde-ronde sebelumnya, belum tertutup.
- **`supabase/verify_fase4.sql` dibuat** untuk menutup gap itu tanpa mengganggu setup panitia-a/b yang sudah dipakai untuk multi-assignee: bikin 2 akun BARU (`panitia-c@test.local`/`panitia-d@test.local`) di 2 instance berbeda, khusus untuk uji isolasi RLS tasks/subtasks + uji akses 404 lintas-instance ke halaman bucket. Langkah-langkahnya lengkap di file itu (cari 2 instance, buat akun Auth, upsert `public.users`, tambah ke Susunan Panitia lewat app buat sekaligus uji dropdown akun-terdaftar, baru tes isolasinya).
- **Fase 4 belum dicentang selesai** di section 8 di atas -- alasan sisa: isolasi RLS tasks/subtasks + akses 404 lintas-instance (poin 6 di `verify_fase4.sql`) belum benar-benar dites lewat UI. Semua bagian lain (board, progress ring, warna status, upload logo, tab menyamping, multi-assignee, dropdown akun terdaftar, "Tugas Saya") sudah dikonfirmasi live oleh Yolanda sepanjang ronde 1-4.

**Penutup verifikasi Fase 4 -- isolasi RLS tasks/subtasks TUNTAS dikonfirmasi.** Akun `panitia-c@test.local` sempat terkendala login (ditelusuri: query `UPDATE ... WHERE email = 'panitia-e@test.local'` yang dicoba Yolanda tidak berpengaruh karena baris dengan email itu belum ada -- `UPDATE` diam-diam no-op kalau tidak ada baris yang cocok, bukan error, jadi gejalanya membingungkan). Diselesaikan dengan bikin akun baru `panitia-e@test.local` (menggantikan `panitia-c`, `INSERT ... ON CONFLICT` bukan `UPDATE`) di instance yang sama dengan `panitia-c` lama (`cc10973d-1e62-43e7-b392-fe3caf3ff904`), tetap berpasangan dengan `panitia-d@test.local` (`b4cac31a-2ad3-4e36-8024-4c28967c279d`) di instance berbeda.

Ketiga langkah verifikasi (poin 5-6 `verify_fase4.sql`) **lolos semua**:
1. panitia-e login, tambah 1 tugas percobaan di instance-nya sendiri.
2. panitia-d login, tugas itu **tidak muncul** di Task Board maupun tab Tugas Saya-nya.
3. panitia-d coba akses langsung URL `/panitia/bucket/<uuid bucket milik instance panitia-e>` lewat address bar (bukan klik dari menu) -- **kena halaman 404**, bukan bocor data instance lain.

**Fase 4 resmi dicentang selesai** di section 8 -- tidak ada yang menggantung. Kalau nanti butuh akun test serupa lagi buat Fase 5+, `panitia-e@test.local`/`panitia-d@test.local` sudah terhubung ke 2 instance berbeda dan siap dipakai ulang tanpa setup baru.

### Fase 5 — Download/Submit Template Budgeting

**Satu migration** `20260810000009_fase5_budget_storage.sql` (sudah dijalankan & dikonfirmasi Yolanda -- tidak ada migration baru lagi di ronde-ronde revisi berikutnya, semua perubahan lanjutan murni kode aplikasi):
1. `alter table budget_submissions add constraint budget_submissions_one_per_instance unique (kepanitiaan_site_id)` -- tabel ini dibuat di Fase 1 tanpa unique constraint (belum ada UI yang menulis ke sana sampai fase ini). Widget budgeting menampilkan status sebagai satu nilai skalar per instance, jadi perlu dijamin maksimal 1 baris per instance supaya `upsert` di `submitBudget` tidak membuat baris ganda kalau panitia upload berkali-kali.
2. Bucket Storage **`budget-template`** -- `public = true` (harus bisa didownload langsung tanpa signed URL kedaluwarsa, sama filosofinya seperti `kepanitiaan-logo` di Fase 4). `allowed_mime_types` dibatasi PDF/XLS/XLSX/DOC/DOCX, `file_size_limit` 10 MB. Policy: baca bebas, tulis (`insert`/`update`/`delete`) cuma leader lewat `public.is_leader()` -- policy ini **tidak peduli path/folder di dalam bucket**, cuma peduli role pemanggil. Ini penting: revisi "template jadi per-event" (poin di bawah) murni pindah folder di dalam bucket yang SAMA, tidak butuh migration baru sama sekali karena policy-nya sudah otomatis mendukung.
3. Bucket Storage **`budget-submission`** -- `public = false`, beda sengaja dari `budget-template`/`kepanitiaan-logo` karena isinya dokumen budget sungguhan milik satu instance, tidak boleh diakses siapa saja lewat URL publik. Path objek selalu `<kepanitiaan_site_id>/submission.<ext>`. Policy pakai helper bawaan Storage `storage.foldername(name)` untuk mengambil segmen folder pertama (`kepanitiaan_site_id`) dan mencocokkannya ke `public.current_user_kepanitiaan_site_id()::text` -- versi Storage dari pola RLS tabel biasa yang sudah dipakai sejak Fase 1, `is_leader()` tetap dapat akses ke semua folder. **Ini TIDAK berubah** di revisi manapun -- submission selalu per `kepanitiaan_site_id` (site-nya), beda dari template yang jadi per `kepanitiaan_id` (event-nya) di revisi 4.

**Kenapa 2 bucket terpisah, bukan 1**: `budget-template` itu leader-managed & bisa dilihat semua orang, `budget-submission` itu panitia-managed & privat per instance -- keduanya butuh aturan public/private dan siapa-boleh-tulis yang berbeda total, jadi tidak masuk akal digabung ke 1 bucket dengan policy yang lebih rumit.

**Revisi 1 -- tombol "kelihatan tidak bisa diklik"**: link download template & "lihat file terupload" awalnya teks biasa dengan underline (`<a className="underline">`), dilaporkan Yolanda kelihatan seperti teks statis bukan elemen interaktif. Diganti jadi `Button asChild` ber-ikon (`Download`/`FileText` dari `lucide-react`) di `components/budget/budget-submission-section.tsx`. Label tombol download persis **"Budgeting Template"** sesuai permintaan Yolanda.

**Revisi 2 -- lokasi widget dipindah keluar dari bucket "Budgeting"**: awalnya `BudgetSubmissionSection` ditempel langsung di halaman detail bucket (`/leader/kepanitiaan/[instanceId]/bucket/[bucketId]` dan `/panitia/bucket/[bucketId]`), berdampingan dengan Task Board. Yolanda minta lokasi berbeda untuk tiap role:
- **Panitia**: `BudgetSubmissionSection` **dipindah** dari halaman bucket ke tab baru **"Submit Budget"** di `/panitia/workspace` (`SideTabs`), jadi tab terakhir -- urutan final: Progres & Task Board | Tugas Saya | Timeline Pelaksanaan | Susunan Panitia | **Submit Budget**. Bucket "Budgeting" di `/panitia/bucket/[bucketId]` sekarang balik jadi bucket Task Board biasa (tidak ada widget nempel lagi) -- panitia masih bisa catat tugas persiapan budget di situ kalau perlu, tapi submit budget-nya sendiri sudah di tab terpisah.
- **Leader**: manajemen template MASTER (sebelumnya komponen `BudgetTemplateSection` di `/leader/dashboard`) dipindah jadi tombol **"Budgeting Template"** di kanan header tiap kartu event di `/leader/kepanitiaan` (lihat Revisi 3 untuk detail). **Widget review submission per-instance untuk leader TIDAK dipindah** -- tetap di halaman detail bucket (`/leader/kepanitiaan/[instanceId]/bucket/[bucketId]`), karena itu satu-satunya tempat leader bisa lihat status & download file submission SATU instance tertentu; permintaan Yolanda cuma menyebut manajemen template master yang perlu pindah, bukan bagian ini.

**Revisi 3 & 4 (dikerjakan sesi yang sama) -- template budgeting jadi PER EVENT**: Yolanda sadar tiap event (`kepanitiaan`, misal "PON") bisa punya kebutuhan template budgeting yang beda dari event lain, jadi 1 file global untuk semua event tidak masuk akal. Perubahan:
- `lib/budget/template.ts` -- `loadBudgetTemplate(supabase, kepanitiaanId)` **(parameter baru)**: `list(kepanitiaanId)` (folder di dalam bucket `budget-template`, bukan `list()` di root bucket lagi), public URL dibangun dari `${kepanitiaanId}/${file.name}`.
- `lib/budget/template-actions.ts` -- `uploadBudgetTemplate(kepanitiaanId, redirectTo, formData)` / `removeBudgetTemplate(kepanitiaanId, redirectTo)` **(parameter baru)**: `clearTemplateFolder` cuma menghapus objek di folder event itu, upload ke `${kepanitiaanId}/template.<ext>`. **Tidak ada migration baru** -- lihat penjelasan policy Storage di atas.
- **PER EVENT, bukan per instance kepanitiaan_site** -- dipilih karena template biasanya sama untuk semua site dalam satu event yang sama (misal FIND @ Ciawi Sentul dan FIND @ Cibitung logisnya pakai template FIND yang sama); kalau dibuat per-instance, leader harus upload berkali-kali untuk event yang sama.
- `components/budget/budget-template-section.tsx` **DIHAPUS** (sudah tidak standalone) -- fungsinya diserap masuk ke `components/kepanitiaan/kepanitiaan-header.tsx`: toggle KEDUA yang independen dari toggle "Edit" (nama/logo) yang sudah ada sejak Fase 4 -- tombol "Budgeting Template" di kanan header tiap kartu event, state lokal `isBudgetOpen` (terpisah dari `isEditing`), membuka panel berisi link download + form upload/ganti/hapus template event itu. Props baru di `KepanitiaanHeader`: `budgetTemplate`, `uploadBudgetTemplateAction`, `removeBudgetTemplateAction`.
- `/leader/kepanitiaan/page.tsx` -- tiap kartu event sekarang me-load template event-nya sendiri lewat `loadBudgetTemplate(supabase, id)`, dipanggil **paralel** untuk semua event via `Promise.all` (bukan query bertingkat/N+1).
- `/panitia/workspace/page.tsx` -- query instance sekarang juga select `kepanitiaan(id, nama)` (sebelumnya cuma `nama`) supaya bisa resolve `kepanitiaanId` untuk load template event yang benar buat tab "Submit Budget".
- `/leader/kepanitiaan/[instanceId]/bucket/[bucketId]/page.tsx` -- tambahan query `kepanitiaan_id` dari tabel `kepanitiaan_site` (sebelumnya halaman ini tidak pernah butuh info kepanitiaan sama sekali) supaya widget review milik leader juga menampilkan template event yang benar, bukan lagi 1 file global.

**Revisi 5 -- alat bantu "Cek Susunan Panitia (Read-only)", SEKALI PAKAI, SUDAH DIHAPUS DARI KODE**: dipicu insiden nyata -- Yolanda melaporkan akun test `panitia-e@test.local` tercatat di Susunan Panitia instance "PON @ Jakarta" tapi tidak bisa login ke situ. **Root cause (bukan bug)**: Susunan Panitia (`committee_members`, daftar nama) dan hak akses login (`public.users.kepanitiaan_site_id`, SATU akun = SATU instance) itu dua hal yang independen -- menambahkan nama ke Susunan Panitia tidak mengubah instance mana yang akun itu bisa login ke situ. Sempat ditambahkan lalu dihapus lagi (jangan cari file-nya, sudah tidak ada di codebase):
- `lib/committee/orphan-check.ts` (`loadOrphanCommitteeMembers`, **dihapus**): cari baris `committee_members` yang emailnya TIDAK cocok akun panitia aktif (`public.users`, role panitia) di instance yang SAMA. Query flat (2x select + 1x `in (...)` untuk nama instance), dicocokkan di JS lewat `Set` `"email|instance_id"` -- tidak N+1, dan tidak butuh RPC karena leader sudah punya akses penuh lewat RLS biasa (`is_leader()` di policy `committee_members_scoped`/`users_select_own_or_leader`, Fase 1).
- `components/committee/orphan-check-section.tsx` (`OrphanCheckSection`, **dihapus**), sempat dipasang di `/leader/dashboard`. **SENGAJA read-only, tidak ada tombol hapus** -- entri Susunan Panitia tidak wajib punya akun login sama sekali (bisa cuma nama tercatat manual tanpa akses sistem), jadi "email tidak cocok" bukan bukti pasti harus dihapus; auto-hapus berisiko menghapus entri yang sebenarnya sah. Tiap baris cuma dapat tombol "Buka Instance" (link langsung ke halaman detail instance terkait) supaya leader bisa cek & hapus manual lewat tombol "Hapus" yang sudah ada di Susunan Panitia.
- `supabase/daftarkan_akun_test_ke_instance.sql` (**masih ada**, tidak dihapus -- ini panduan reusable, beda dari 2 file di atas yang cuma alat bantu sekali pakai) -- panduan + query `update public.users set kepanitiaan_site_id = ...` siap pakai untuk mengikat ulang akun test ke instance yang benar. Dipakai untuk memperbaiki kasus panitia-e di atas. Hasil akhir yang dikonfirmasi Yolanda: **panitia-a & panitia-b -> FIND @ Ciawi Sentul** (`a545cf0c-3d63-4112-b405-7384e76dec24`, berdua di instance yang sama), **panitia-d & panitia-e -> PON @ Jakarta** (`cc10973d-1e62-43e7-b392-fe3caf3ff904`, berdua di instance yang sama juga) -- sengaja 2-2 di instance yang sama, mengikuti pola pengujian "2 orang di 1 instance" yang sudah dipakai Fase 4 ronde 5.
- **Penyelesaian**: Yolanda sudah pakai `OrphanCheckSection` untuk menemukan & menghapus manual semua entri usang panitia-a/b/d/e di instance-instance lama (FIND @ Cibitung, Habiha @ Ciawi) lewat tombol "Hapus" di Susunan Panitia. Setelah beres, Yolanda minta widget-nya dihapus lagi dari `/leader/dashboard` (bukan fitur permanen) -- selesai dikerjakan, `/leader/dashboard` balik seperti sebelum Revisi 5 (tanpa card ini).

**File placeholder** `supabase/seed/template-budgeting-placeholder.xlsx` -- dibuat pakai skill `xlsx` (openpyxl): kolom Kategori/Item/Jumlah/Satuan/Harga Satuan/Total/Catatan, 3 baris contoh berlabel "(contoh)" + beberapa baris kosong buat diisi + baris TOTAL dengan formula `SUM`. **Catatan penting**: langkah wajib skill xlsx untuk recalculate formula lewat LibreOffice (`scripts/recalc.py`) **gagal dijalankan di sandbox ini** -- `soffice` tidak bisa memuat file apapun sama sekali (dicoba juga dengan file `.txt` trivial, gagal dengan error yang sama, jadi ini keterbatasan environment, bukan file-nya yang korup). Konsekuensinya: file ini tidak punya cached value untuk formulanya sampai dibuka pertama kali di Excel/Google Sheets (yang otomatis recalculate saat file dibuka) -- tidak masalah untuk pemakaian normal. Ini cuma placeholder sementara per event -- ganti dengan file asli kapan saja lewat tombol "Ganti" di kartu event masing-masing begitu tersedia.

**Verifikasi yang sudah dilakukan (di sandbox, bukan live)**: `next build`, `next lint`, `tsc --noEmit` -- bersih di setiap ronde revisi (dicoba ulang tiap kali ada perubahan kode), pola `.env.local` placeholder sementara sama seperti fase-fase sebelumnya (dihapus lagi sebelum commit, tidak pernah masuk git). Koneksi nyata ke Supabase tetap tidak bisa dites dari sandbox ini.

**Verifikasi live yang SUDAH dikonfirmasi Yolanda**: migration `20260810000009` jalan sukses; ketiga PR (widget-clickable-fix, relokasi widget, template-per-event) sudah di-review lewat preview Vercel dan di-merge ke `main`; akun panitia-a/b/d/e sudah diikat ke instance yang benar lewat `daftarkan_akun_test_ke_instance.sql`.

**Verifikasi live yang BELUM dikonfirmasi -- checklist ini WAJIB dituntaskan sebelum Fase 5 dicentang selesai di section 8** (lihat juga catatan "Fase 6 boleh paralel" di section 8):
1. Login leader -> buka Manajemen Kepanitiaan -> klik tombol "Budgeting Template" di kartu salah satu event -> upload `supabase/seed/template-budgeting-placeholder.xlsx` (atau file asli kalau sudah ada) -> pastikan nama file muncul sebagai link download.
2. Buka event LAIN -> pastikan tombol "Budgeting Template"-nya independen (belum ada template sendiri, tidak ikut template event pertama) -- ini bukti template benar-benar per-event, bukan global lagi.
3. Login sebagai panitia di salah satu instance milik event yang templatenya sudah diisi -> buka `/panitia/workspace` -> tab "Submit Budget" -> pastikan tombol "Budgeting Template" mengarah ke file yang benar (milik event ini, bukan event lain).
4. Di tab yang sama, upload file submission (PDF/Excel/Word apapun) -> pastikan status berubah jadi "Lengkap", tombol "Lihat File Terupload" muncul dan bisa dibuka.
5. Upload file KEDUA dengan ekstensi berbeda ke instance yang sama -> pastikan file lama tidak menumpuk di Storage (cek Dashboard > Storage > budget-submission > folder instance itu, harus tetap 1 objek).
6. Login sebagai panitia di instance LAIN -> pastikan status budgeting-nya independen (tidak ikut "Lengkap" walau instance tetangga sudah), dan tombol "Budgeting Template"-nya menunjuk template event MILIKNYA sendiri.
7. Login balik sebagai leader -> buka bucket "Budgeting" instance yang panitia sudah upload submission-nya (`/leader/kepanitiaan/[instanceId]/bucket/[bucketId]`) -> pastikan leader BISA lihat status "Lengkap" dan BISA buka "Lihat File Terupload".
8. Coba upload file dengan format di luar PDF/Excel/Word atau >10MB (baik di form template maupun submission) -> pastikan ditolak dengan pesan error jelas, bukan error mentah.

(Poin 9 versi sebelumnya -- cek alat bantu "Cek Susunan Panitia" -- sudah tidak berlaku, alat itu sudah dipakai & dihapus lagi, lihat Revisi 5 di atas.)

Setelah kedelapan langkah di atas lolos, centang Fase 5 selesai di section 8.

### Fase 6 — Dashboard Kepanitiaan (Leader)

**Tidak ada migration baru di fase ini.** Dashboard murni membaca ulang tabel yang sudah ada (`kepanitiaan_site`, `buckets`, `tasks`, `subtasks`, `committee_members`, `budget_submissions`) lewat helper yang sudah dibuat di fase-fase sebelumnya -- tidak ada kolom/tabel baru di model data section 5.

**`lib/dashboard/leader-overview.ts`** (baru):
- `loadInstanceGroups(supabase)` -- semua instance `kepanitiaan_site`, dikelompokkan per `kepanitiaan` (event), diurutkan by nama event lalu nama site (beda dari `/leader/kepanitiaan` yang urut by `id`/insert order -- checklist dashboard sengaja dibuat mudah ditelusuri kalau jumlah event/site sudah banyak). Dipakai untuk render checklist multi-select.
- `loadInstanceOverview(supabase, instance)` -- ringkasan 1 instance: progres gabungan tugas+subtugas (**reuse `loadBucketBoardData`** dari Fase 4, angka yang sama persis dengan yang dilihat panitia/leader di halaman workspace/board -- sengaja tidak dihitung ulang dengan cara berbeda), progres per bidang (dari `progressByBucket` yang sama), dan status budgeting (**reuse `loadBudgetSubmission`** dari Fase 5). Dipanggil sekali per instance yang dicentang leader (`Promise.all`, paralel).

**`lib/budget/format.ts`** (baru, refactor kecil): `BUDGET_STATUS_LABEL`/`BUDGET_STATUS_BADGE_CLASSNAME` diekstrak dari `budget-submission-section.tsx` (sebelumnya konstanta lokal di file itu) supaya kartu perbandingan Fase 6 bisa pakai warna/label status budgeting yang identik tanpa duplikasi. `budget-submission-section.tsx` diupdate untuk import dari sini (`as STATUS_LABEL`/`as STATUS_CLASSNAME` supaya nama variabel di file itu tidak berubah, jadi diff-nya minimal).

**Mekanisme multi-select -- form GET tanpa JS, bukan `useState`/client component**: mengikuti prinsip "nol client-side JS kalau bisa" yang sudah dipegang sejak susunan panitia Fase 2 dan bucket Fase 4. Checkbox `name="instance"` (bisa banyak, value = id instance) di dalam `<form method="get">` **tanpa atribut `action`** -- browser otomatis submit ke URL halaman saat ini dengan semua checkbox terpilih sebagai query string (`?instance=<id1>&instance=<id2>`), Next.js App Router membaca ini lewat `searchParams` di halaman (Server Component, bukan `useSearchParams()` client hook). Konsekuensinya: pilihan instance **ter-bookmark di URL** (bisa di-refresh/share link-nya tanpa kehilangan pilihan) -- efek samping yang tidak diminta eksplisit tapi cocok dengan HANDOVER section 6 ("leader bisa pilih beberapa instance sekaligus dan lihat perbandingan progres side-by-side").
- `components/dashboard/instance-picker.tsx` (`InstancePicker`) -- render checklist-nya, dikelompokkan per event dengan heading, checkbox `defaultChecked` dari `selectedIds` supaya state di URL tercermin balik ke tampilan setelah submit.
- `components/dashboard/instance-overview-card.tsx` (`InstanceOverviewCard`) -- satu kartu perbandingan per instance terpilih, lebar tetap (`w-72`, pola sama seperti kolom `BucketBoard` Fase 4 dan bar Gantt Fase 3) supaya beberapa kartu bisa disejajarkan dalam baris `overflow-x-auto` untuk perbandingan "side-by-side" sesuai kriteria selesai Fase 6. Isinya: `ProgressRing` (reuse dari Fase 4) untuk progres gabungan, badge status budgeting (reuse `lib/budget/format.ts`), daftar progres per bidang (nama bidang + `ProgressBar` reuse + persen), dan link "Buka Instance" ke `/leader/kepanitiaan/[instanceId]` untuk drill-in kalau leader mau lihat detailnya.

**`app/(leader)/leader/dashboard/page.tsx`** (diubah, sebelumnya cuma placeholder teks "akan diimplementasikan di Fase 6"): menambahkan `InstancePicker` + baris kartu perbandingan (kalau ada instance yang dicentang) di bawah tombol "Kelola Kepanitiaan" yang sudah ada. Parsing `searchParams.instance` (bisa `string | string[] | undefined` dari Next.js kalau ada 0/1/banyak checkbox tercentang) dinormalisasi jadi array dulu sebelum difilter ke `loadInstanceGroups()` -- id yang tidak valid (misal instance sudah dihapus tapi URL lama masih dibookmark) otomatis diabaikan karena filter-nya cocokkan ke daftar instance yang benar-benar ada, tidak trust langsung ke query string.

**Kenapa dikerjakan sebelum Fase 5 dicentang selesai**: instruksi langsung Yolanda di sesi ini ("saat ini kita akan start di fase 6"), sesuai izin eksplisit "Fase 6 boleh dikerjakan lebih dulu, PARALEL dengan checklist Fase 5" di section 8 -- bukan pelanggaran urutan section 6 secara diam-diam. Tidak ada masalah teknis: migration `...0009` sudah dikonfirmasi jalan (lihat section 8), dan Fase 6 cuma **membaca** `budget_submissions` (tidak menulis) sehingga akan tetap konsisten dipakai terlepas dari itu.

**Catatan sinkronisasi**: sesi ini awalnya mengira Catatan Teknis Fase 5 usang (ditemukan lewat `git log` sebelum sesi ini di-rebase ke `main` terbaru) dan sempat menulis ulang sebagian isinya sendiri -- setelah rebase, ternyata sesi lain (PR #7) sudah memperbaikinya lebih dulu dengan detail yang lebih lengkap (5 ronde revisi, termasuk riwayat alat bantu "Cek Susunan Panitia" yang sempat ditambah lalu dihapus). Versi Fase 5 di atas adalah **punya PR #7**, bukan tulisan ulang sesi ini -- dipertahankan karena lebih akurat.

**Verifikasi yang sudah dilakukan (di sandbox, bukan live)**: `npm install`, `next build`, `next lint` -- semua bersih dengan `.env.local` placeholder sementara (dihapus lagi sebelum commit, tidak pernah masuk git, pola sama seperti fase-fase sebelumnya). Koneksi nyata ke Supabase (isi checklist multi-select dengan data instance sungguhan, angka progres yang benar) tetap tidak bisa dites dari sandbox ini.

**Langkah verifikasi live yang harus dilakukan Yolanda** (syarat centang Fase 6 di section 8):
1. Login sebagai leader, buka `/leader/dashboard` -- pastikan card "Pilih Instance untuk Dibandingkan" muncul dengan checklist dikelompokkan per event, masing-masing menampilkan nama site sebagai pilihan checkbox.
2. Centang 2-3 instance dari event yang SAMA maupun event yang BEDA (mis. "FIND @ Cibitung" + "PON @ Ciawi"), klik "Tampilkan Perbandingan" -- pastikan muncul baris kartu yang bisa di-scroll horizontal, masing-masing menampilkan nama event+site yang benar, ring persentase, dan progres per bidang instance itu (cocokkan angkanya dengan yang dilihat langsung di workspace/board instance tersebut).
3. Refresh halaman (atau copy-paste URL-nya ke tab baru) -- pastikan instance yang tadi dicentang **tetap tercentang** dan kartunya tetap tampil (bukti pilihan tersimpan di URL, bukan cuma state sementara).
4. Cek badge status Budgeting di tiap kartu -- untuk instance yang panitia-nya sudah submit lengkap (kalau Fase 5 sudah diverifikasi), badge harus "Lengkap"; untuk yang belum, "Belum Lengkap".
5. Klik "Buka Instance" di salah satu kartu -- pastikan masuk ke halaman detail instance (`/leader/kepanitiaan/[instanceId]`) yang benar, bukan instance lain.
6. Uncheck semua instance dan submit ulang -- pastikan baris kartu perbandingan hilang (kembali ke tampilan checklist saja, tidak ada kartu kosong/error).

Setelah keenam langkah di atas lolos, centang Fase 6 selesai di section 8 dan lanjut ke **Fase 7 — Notifikasi**.
